# Exterminate Zombies 测试链路与 Agent 实景验收规范

> 版本：1.1
>
> 生效日期：2026-08-10
>
> 适用对象：开发者、代码审查者、编码 Agent、测试 Agent

## 1. 文档定位

本文件是本项目测试决策、Agent 浏览器操作和实景验收的根级规范，负责回答：改动后最低要测到哪一层、什么时候必须进入真实场景、使用什么工具、留下什么证据、可以得出什么结论。

文档职责与优先级如下：

1. 用户当前指令与上层 Agent 规则文件高于本文件。当前实际生效的上层规则是用户全局 `CLAUDE.md`；仓库暂无项目级 `CLAUDE.md` / `AGENTS.md`，若未来新增则同样适用本条。
2. 本文件规定测试分层、触发条件、工具选择、执行流程和结论口径。
3. [`docs/TESTING.md`](docs/TESTING.md) 提供详细玩法检查项和现有 CDP 冒烟示例。
4. [`docs/execution/`](docs/execution/) 记录单次复杂任务的测试计划、实际结果和遗留风险。
5. 规则冲突时遵循更高优先级文件；同级规则冲突时采用验证更充分、结论更保守的一项。

快速使用路径：

1. 确定改动类型 → 第 5 章决策树得出最低验证层级。
2. 需要执行命令或浏览器验证 → 第 3 章完成授权门禁（仅 V1 时可用 3.4 轻量确认）。
3. 执行实景测试 → 第 9 章选工具，第 10 章 Canvas 规则，第 11 章场景矩阵，第 12 章标准流程。
4. 得出结论并汇报 → 第 13 章结论等级，第 14 章记录模板。

本文件中的用词：

- **必须**：不满足就不能把对应验收项标记为通过。
- **应**：默认执行；只有存在明确原因时才可跳过，并要记录原因。
- **可以**：按任务风险选择，不构成最低门槛。

## 2. 核心原则

1. **验证强度必须匹配风险**：低层验证不能替代高层验收。单元测试通过不代表游戏能加载，截图正常不代表交互正确，成功进入关卡不代表关卡可通关。
2. **用户路径优先**：需要证明菜单、解锁、存档、输入或场景流转时，必须走玩家实际可执行的路径。直接跳场景只能缩短无关前置，不能证明被绕过的流程。
3. **Canvas 使用联合证据**：本项目主要界面位于 Phaser Canvas，验收必须组合真实键鼠输入、画面证据和运行时只读探针，不能只依赖 DOM 定位器。
4. **先定义通过标准再操作**：每次实景测试开始前，必须写清目标场景、操作步骤、预期状态、视觉判据和终止条件。
5. **证据必须可复核**：结果至少包含环境、步骤、运行时错误、关键状态和截图；动态机制应包含前、中、后三个时点或视频/trace。
6. **测试代码不能替产品通过**：状态注入、测试开关、跳关和时间加速只能作为测试前置，必须显式标记；它们不能证明正式掉率、解锁、经济或完整流程。
7. **失败不得被重试掩盖**：出现异常后先保存首个失败证据，再从干净状态复跑一次以区分稳定缺陷与偶发问题。禁止反复重试直到出现一次通过就结束。
8. **结论不得越界**：Agent 可以验收客观交互和运行状态，但不能替代真人对手感、乐趣、疲劳、混音和难度是否舒适的主观判断。

## 3. 命令与授权边界

测试执行继续服从项目全局规则：

1. 未经用户明确同意，不执行 `npm test`、`npm run typecheck`、`npm run build`、端到端测试或其它测试命令。
2. 需要命令验证时，Agent 必须先说明原因、拟执行命令、预计耗时和覆盖范围，等待用户明确同意。
3. 用户明确要求“用浏览器验收/试玩本次改动”表示已指定测试目标，但不跳过前提条件确认门禁；Agent 仍须提交确认单并等待启动批准，且范围不自动包含 Vitest、类型检查或构建。
4. 用户明确要求“运行全部测试”表示已指定测试范围，但不跳过前提条件确认门禁；Agent 仍须说明依赖、准确命令和副作用，且范围不自动包含类型检查、构建或依赖安装。
5. 不得因测试需要擅自修改 `package.json`、安装 Playwright、下载浏览器运行时或增加 `--no-save` 依赖。需要时必须单独说明影响并取得同意。
6. 用户未授权必须执行的验证时，Agent 应完成静态审阅，列出建议命令和未验证风险；不得把“未执行”写成“通过”。

### 3.1 测试前提条件确认门禁

执行任何 V1-V6 验证前，Agent 必须先完成前提条件调查，向用户提交“测试前提条件确认单”，并停下来等待用户明确同意。未得到回复或回复没有覆盖拟执行动作时，不得开始测试。

门禁期间只允许进行不会启动测试或改变环境的 V0 只读调查，例如阅读 `package.json`、lockfile、测试配置和相关源码，检查已安装工具的版本、文件是否存在、端口是否占用。以下动作全部必须放在用户批准之后：

1. 安装、升级、降级或删除任何项目/系统依赖。
2. 执行 `npm ci`、`npm install`、`npx playwright install` 或其它会下载内容的命令。
3. 启动 Vite、预览服务器、Chrome 调试实例或其它后台进程。
4. 打开并操作测试页面、连接 Browser、Playwright 或 CDP 执行 V3-V5。
5. 执行 Vitest、类型检查、构建、E2E、性能测试或完整试玩。
6. 修改测试开关、存档、浏览器数据、端口配置或生成测试夹具。

即使依赖齐全、服务器已经运行、无需新增工具，Agent 也必须明确写出“无需安装新依赖”，并在获得用户同意后才开始本轮测试。

### 3.2 必须说明的前提条件

确认单至少覆盖以下项目；不适用的项目写“无”或“不适用”，不能省略后让用户猜测：

| 类别 | 必须说明的内容 |
| --- | --- |
| 测试目标 | 本次要验证什么、不验证什么、计划执行 V1-V6 中哪些层级 |
| 基础环境 | 操作系统、Node.js/npm 要求、目标浏览器及版本、视口/DPR、是否需要有头浏览器或音频设备 |
| 项目依赖 | `node_modules` 是否可用、是否需要 `npm ci`，涉及的 Vitest/Vite/TypeScript 等现有依赖 |
| 额外工具 | 是否需要集成 Browser、独立 Playwright、浏览器运行时、Chrome/CDP、`ws` 或其它工具 |
| 安装影响 | 每个待安装包的名称/用途、准确命令、是否修改 `package.json`/lockfile、是否下载浏览器、网络/磁盘影响 |
| 服务与端口 | 是否需要启动开发/预览服务器、拟使用命令、URL、端口和预计保留时长 |
| 配置与数据 | 环境变量、账号/权限、测试存档、localStorage、测试开关、种子数据及可能产生的副作用 |
| 执行命令 | 按顺序列出准备运行的完整命令；浏览器试玩则列出目标场景和关键操作 |
| 时间与产物 | 预计耗时、截图/日志/trace 保存位置、结束后的清理方式 |
| 阻塞与降级 | 缺少条件时的替代方案，以及降级后哪些结论不能得出 |

当前项目的基础依赖是 `package.json` 中的 Phaser、Vite、TypeScript 和 Vitest；独立 Playwright、Playwright 浏览器包和临时 CDP `ws` 依赖均不是默认已具备能力，必须按实际环境检查后报告，禁止猜测已经安装。

### 3.3 用户确认与变更控制

1. Agent 必须用明确问题结束确认单，例如：“是否同意按以上前提安装依赖并开始测试？”
2. 用户可以只批准其中一部分。Agent 只能执行已批准的命令、工具、场景和范围。
3. 一次回复可以同时批准依赖安装和后续测试，但确认单必须把两类动作分开列明。
4. 如果用户只批准安装依赖，安装完成后 Agent 必须汇报结果并再次询问，不能自动开始测试。
5. 执行中新增依赖、命令、浏览器、场景、数据修改或明显延长耗时时，原批准不再覆盖新增部分，必须暂停并重新咨询。
6. 发现前提条件与确认单不一致时，必须先报告差异；不能为了继续测试而自行修复环境。

统一确认模板：

```md
## 测试前提条件确认

- 测试目标：
- 计划层级：V_
- 不在本轮范围：
- 基础环境：
- 现有依赖状态：
- 需要安装/调整：无 / <依赖、用途、准确命令和文件影响>
- 浏览器与工具：
- 服务与端口：
- 配置、账号与测试数据：
- 计划命令（按执行顺序）：
- 实景场景与操作：
- 预计耗时：
- 证据与临时产物：
- 清理方式：
- 缺失条件与降级影响：

是否同意按以上前提准备环境并开始测试？
```

### 3.4 仅 V1 的轻量确认

本轮只执行 V1（Vitest）且同时满足以下全部条件时，可以用轻量确认代替 3.2 全表：

1. 不安装、升级或删除任何依赖，`node_modules` 已确认可用。
2. 不启动开发服务器、浏览器或任何常驻进程。
3. 不修改测试开关、存档、配置或任何仓库文件。

轻量确认只需说明拟执行命令（含测试文件范围）、选择这些文件的原因和预计耗时，例如：

```md
## V1 轻量确认

- 命令：npm test -- tests/enhancements.test.ts tests/config-integrity.test.ts
- 原因：本次改动 src/config/enhancements.ts，上述文件覆盖其配置约束与交叉引用
- 预计耗时：约 1 分钟

是否同意执行？
```

轻量确认同样必须等待用户明确同意后才能执行；任一条件不满足，或计划包含 V2 及以上层级时，仍使用 3.2 全表确认单。受影响测试的选择参考第 15 章的测试覆盖映射。

## 4. 验证分层

V0 是所有任务的共同基础；V1/V2 只在对应逻辑或工程门槛适用时加入；浏览器证据通常按 V3 -> V4 -> V5 累计，V6 不能替代前面的可重复验证。

| 层级 | 名称 | 主要手段 | 能证明什么 | 不能证明什么 |
| --- | --- | --- | --- | --- |
| V0 | 静态审阅 | 代码、类型、配置、调用链、差异检查 | 实现与项目结构一致，字段和上下游可解释 | 命令可通过、游戏可运行 |
| V1 | 定向自动化 | Vitest，优先运行受影响测试 | 纯逻辑、配置约束、状态转换的确定性结果 | Phaser 生命周期、真实渲染和输入 |
| V2 | 工程门槛 | `typecheck`、必要时 `build` | TypeScript 与生产打包入口可通过 | 关卡可玩、画面正确 |
| V3 | 浏览器运行时冒烟 | Browser / Playwright / CDP，控制台与资源监听 | 页面可加载、场景可启动、资源可取、无运行时异常 | 真实交互链、碰撞、节奏与完整玩法 |
| V4 | Agent 实景交互 | 有真实画面的浏览器、键鼠操作、截图、只读探针 | 玩家可执行的客观交互和对应状态/画面变化 | 完整关卡可完成、主观手感 |
| V5 | Agent 完整场景试玩 | 连续操作目标关卡/流程直到明确结算 | 完整波次、Boss、结算、解锁或长链路能闭环 | 真人是否觉得好玩、舒适、清晰 |
| V6 | 真人与发布验收 | 有头浏览器人工试玩、多浏览器/设备检查 | 手感、听感、难度、可理解性和发布体验 | 不能替代可重复自动化回归 |

`docs/TESTING.md` 中原有 L1 自动化检查横跨本文件 V1（Vitest）与 V2（类型检查），原有 L2 冒烟对应本文件 V3；原有 L3 清单应拆分处理：客观逻辑项由 V4/V5 执行，主观手感项由 V6 执行。

## 5. 最低验证决策树

按改动的最高风险选择层级，不按文件数量选择：

1. 只改文档、注释、许可台账，且不改变运行时输入：最低 V0。
2. 只改纯函数、数学工具、配置校验或可确定状态转换，且不改变导出签名、类型定义或跨文件调用契约：最低 V0 + V1；改变上述契约时按第 3 条升级。
3. 改动构建入口、类型契约、依赖或生产资源解析：最低 V0 + V1 + V2 + V3。
4. 改动场景创建/销毁、资源预加载、事件监听、对象池或浏览器 API：最低 V0 + V1（适用时）+ V3。
5. 改动玩家可见画面、HUD、菜单、输入、动画、音频触发、碰撞、投射物、掉落或暂停：最低 V0 + V3 + V4。
6. 改动关卡空间、波次、Boss、敌群组合、掉率、经济、强化节奏或难度：最低 V0 + V3 + V4 + V5；涉及“是否舒适/合理”还必须 V6。
7. 改动性能、长时间状态、存档迁移、跨场景恢复或发布行为：按目标执行 V4/V5，并在发布前补 V6。

无法确定时向上提升一级。多个条件同时命中时取最高层级，并合并必要的低层验证。

注意：Vitest 不执行 TypeScript 类型检查，V1 通过不能证明类型无回归。任何 `.ts` 源码改动在已获得命令授权时，应随 V1 附带 `npm run typecheck`；未获授权时必须把类型回归列入未验证风险。

## 6. 必须实景测试的场景

出现以下任一情况，不能只做代码审阅或自动化测试，必须至少执行 V4：

1. 修改 `src/scenes/` 中任何玩家可交互或可见行为。
2. 修改玩家移动、瞄准、射击、换弹、切枪、道具、暂停或键位绑定。
3. 修改碰撞体、障碍物、子弹命中、范围伤害、连锁爆炸或敌方投射物。
4. 修改动画帧、朝向、origin、缩放、深度、枪口位置、命中区域或 Canvas 布局。
5. 新增或替换运行时图片、音频、字体、关卡环境或预加载资源。
6. 修改 HUD 提示、Boss 血条、战斗警报、卡片选择、图鉴或设置界面。
7. 修改场景切换、暂停/恢复、挂起战局、Game Over、Level Clear 或本地存档行为。
8. 修改波次生成、特殊敌人能力、Boss 阶段、掉落拾取或强化生效。
9. 问题只能在 Phaser 生命周期、浏览器音频解锁、实际缩放或连续帧更新中出现。
10. 用户明确要求“实机”“实景”“浏览器验收”“Agent 自己玩”或“完整试玩”。

以下改动通常不要求实景测试，但仍需 V0，且有测试覆盖时应执行 V1：

1. 不影响运行时行为的文档和注释。
2. 与 Phaser、DOM、存档和时间无关的纯数学/数据转换函数。
3. 只增加自动化测试且未改生产逻辑。
4. 机械化格式调整，确认生成结果语义完全不变。

## 7. 什么时候必须让 Agent 完整玩关卡

满足以下任一条件时，目标不是“进入场景并截图”，而是 V5 完整场景试玩：

1. 改动了目标关卡的波次、敌群、场景物、障碍布局、Boss 或通关条件。
2. 改动会随时间累积，例如弹药经济、掉落节奏、强化叠加、对象池复用、计时器或无尽模式压力。
3. 需要证明从开始战斗到 `LevelClearScene`、`GameOverScene` 或无尽目标波次的闭环。
4. 需要验证通关解锁、最佳波次、本地存档、暂停返回主页再恢复等跨场景结果。
5. 缺陷发生在后续波次、Boss 阶段转换、死亡结算或重复进入场景之后。
6. 用户明确要求 Agent “打完”“通关”“玩一局”或评价完整关卡流程。

完整试玩的判定规则：

1. Agent 必须通过真实键鼠输入持续移动、瞄准、射击、换弹、切枪或使用道具，不能只让角色原地等待。
2. 直接调用 `scene.start` 可以进入目标关卡，但不能证明主菜单选择、关卡解锁或返回流程。
3. 直接生成 Boss、修改生命、清空敌人或跳波只能验收单个机制，结果必须标为“定向机制测试”，不能标为“完整关卡通过”。
4. 完整关卡验收应以真实结算场景和对应状态为终点；中途死亡只证明失败链路正常，不能证明关卡可通关。
5. 随机性显著时应至少复跑一次关键流程，或明确记录只覆盖了本次随机样本。

## 8. Agent 与真人验收边界

Agent 可以独立完成：

1. 页面加载、菜单进入、场景切换、暂停恢复和结算路径。
2. 键鼠输入是否生效，状态是否按预期变化。
3. 碰撞、命中、掉血、掉落、拾取、Boss 技能和波次推进等客观行为。
4. 画面是否非空，元素是否溢出、遮挡、闪烁、错位或缺失。
5. 控制台异常、失败请求、资源解码、活动场景、对象数量和公开状态探针。
6. 长时间运行、重复进出场景和可量化性能数据。

以下结论必须由真人完成或复核：

1. 移动、瞄准、枪感、躲避窗口和难度是否舒适。
2. 武器、Boss、强化和关卡是否有趣、紧张或公平。
3. 音量、混音、声像、音色辨识度和长时间听感。
4. 信息是否能被首次接触游戏的玩家自然理解。
5. 外部试玩者是否能独立完成一局。
6. 目标浏览器和真实显示设备上的最终观感。

Agent 完成全部客观项但缺真人主观项时，结论只能写“客观实景验收通过，真人主观验收待完成”。

## 9. 工具选择顺序

### 9.1 会话内集成 Browser：默认首选

当前 Agent 环境提供 Browser 能力时，应先读取并遵循适用的 Browser skill，再建立或复用浏览器连接。它适合：

1. 打开本地开发服务器并操作有真实画面的页面。
2. 发送键盘、鼠标、滚轮输入。
3. 采集截图、控制台、网络和页面状态。
4. 通过其 Playwright 风格接口执行短小、持续的浏览器步骤。

集成 Browser 已提供浏览器运行时，不等于项目已安装 Playwright，不得因此修改仓库依赖。

### 9.2 独立 Playwright：重复脚本化时使用

当场景需要循环、条件分支、精确输入、trace、视频、截图对比或后续进入 CI 时，优先考虑 Playwright。使用前必须确认：

1. 当前环境确实已经提供可用 Playwright，而不是只在 lockfile 中出现可选传递依赖。
2. 仓库当前未在 `package.json` 中直接声明 Playwright，也没有正式 `playwright.config.*`。
3. 安装 `@playwright/test`、下载浏览器或提交 E2E 配置都属于独立工程改动，必须取得用户同意。
4. Canvas 内的 Phaser 对象不能用普通文本/角色 DOM locator 定位；Playwright 主要负责真实输入、截图、浏览器事件和运行时 evaluate。

### 9.3 CDP：现有兼容降级方案

集成 Browser 和 Playwright 均不可用，但本机 Chrome 可远程调试时，可以按 `docs/TESTING.md` 使用 CDP：

1. 订阅 `Runtime.exceptionThrown`。
2. 导航到本地 Vite 页面并等待场景稳定。
3. 用 `Runtime.evaluate` 读取 `window.__GAME__` 的公开状态。
4. 用 `Input.dispatchKeyEvent` / `Input.dispatchMouseEvent` 发送真实输入。
5. 用 `Page.captureScreenshot` 保存画面证据。

CDP 不得退化成只调用内部方法和截图。需要验收交互时，仍要发送真实键鼠事件。

### 9.4 人工浏览器：主观与发布验收

V6 使用有头浏览器和真实音频设备。公开试玩前应覆盖实际声明支持的桌面浏览器；只跑 Chromium 不得写成“多浏览器通过”。

### 9.5 工具不可用时

1. 在声明 Browser 不可用前，必须先检查当前会话提供的 browser skill 和浏览器连接方式。
2. 不得未经同意自行安装新依赖或换用会修改用户系统状态的桌面自动化工具。
3. 无法建立浏览器连接时，完成 V0 和已获授权的低层验证，记录具体阻塞点、未覆盖场景和建议补测方式。
4. 工具不可用属于“未执行/受阻”，不是产品失败，也不是验收通过。

## 10. Phaser Canvas 专项规则

### 10.1 定位与输入

页面只有 `#game` 容器和 Canvas。Phaser 文本、按钮、角色、子弹和 HUD 不是独立 DOM 节点，因此：

1. DOM locator 只用于确认 `canvas` 存在、可见和尺寸稳定。
2. 菜单与战斗优先使用项目公开键位；需要点击时按 Canvas 边界换算逻辑坐标。
3. 逻辑坐标固定为 `1280 x 720`，高分屏只改变渲染缓冲区，不改变游戏坐标。
4. 逻辑点到页面点的换算为：

```text
pageX = canvasRect.left + logicalX / 1280 * canvasRect.width
pageY = canvasRect.top  + logicalY / 720  * canvasRect.height
```

5. 发送键盘前应先让 Canvas 获得焦点；射击前应先移动指针确定瞄准方向。
6. 每个用例结束时必须释放仍按下的键和鼠标按钮，避免影响后续用例。
7. 不得凭截图估算碰撞是否正确；必须实际推动玩家/敌人/投射物接触目标并观察位置或状态变化。

### 10.2 开发运行时探针

开发模式下 `src/main.ts` 会暴露 `window.__GAME__`。探针默认只读，用来辅助证明场景和公开状态，不替代玩家输入。

前置条件：`GameScene.getState()` 的内部状态在场景启动时才初始化（`src/scenes/GameScene.ts` 中 `state` 为延迟赋值字段），GameScene 从未启动时执行下方示例会因解引用未初始化状态而抛错。主菜单等阶段只读取 `game.scene.isActive(...)` 一类场景管理器信息；完整探针必须在确认 GameScene 已启动后执行。

推荐读取以下已确认入口：

```js
(() => {
  const game = window.__GAME__;
  const gameScene = game.scene.getScene('GameScene');
  const state = gameScene.getState();

  return {
    gameActive: game.scene.isActive('GameScene'),
    hudActive: game.scene.isActive('HUDScene'),
    mode: state.mode,
    levelId: state.levelId,
    waveIndex: state.waveIndex,
    health: state.player.health,
    currentWeaponId: state.player.currentWeaponId,
    ownedWeapons: [...state.player.ownedWeapons],
    activeEnhancements: [...state.player.activeEnhancements],
    pauseReason: gameScene.getPauseReason(),
    children: gameScene.children.list.length,
  };
})()
```

规则：

1. 只使用源码已经确认的字段和公开方法，禁止猜测多个字段兜底。
2. 默认不读私有字段，不直接调用伤害、生成、结算等内部方法。
3. 需要状态注入时必须在用例中单独标记“测试前置”，说明绕过了哪些真实流程。
4. `window.__GAME__` 只在开发模式存在，不得把生产构建缺少该对象判为缺陷。

### 10.3 画面证据

每个 V4/V5 用例至少保留：

1. 进入目标场景后的基线截图。
2. 关键操作发生时或发生后的结果截图。
3. 最终状态/结算截图。
4. 控制台异常、`pageerror`、失败请求和关键资源响应摘要。
5. 对应只读状态探针结果。

动态机制如冲刺、轰炸、换弹、爆炸、暂停或阶段转换，应使用前/中/后截图、短视频或 trace。单张最终截图不能证明动画过程和时序正确。

Canvas 像素检查至少确认：

1. 画布不是纯黑、纯背景或透明空画面。
2. 交互前后目标区域存在可解释的像素变化。
3. Canvas 没有被浏览器裁切，HUD 与战场关键区域处于可见范围。
4. 截图应按实际画布检查，而不是只看页面 DOM 尺寸。

### 10.4 音频证据

自动化可以证明资源已加载、用户手势后触发播放、暂停/恢复状态正确和控制台没有解码错误；不能证明音量合适、声像自然或武器声音好听。后者必须进入 V6。

## 11. 场景覆盖矩阵

| 改动区域 | 最低实景场景 | 必测路径 |
| --- | --- | --- |
| Boot / Preload / 资源 | 冷启动到主菜单，再进入一个关卡 | 资源请求、解码、主菜单和关卡均无异常 |
| 主菜单 / 解锁 | 主菜单 + 目标关卡 + 返回 | 选择、锁定状态、开始、结算后解锁、刷新后持久化 |
| GameScene / HUD / 通用输入 | 第一关 + 无尽模式 | 移动、瞄准、射击、换弹、切枪、HUD 同步、暂停恢复 |
| 武器 / 投射物 | 第一关或能稳定复现的目标关卡 | 每把受影响武器的开火、命中、弹药、换弹和特殊效果 |
| 敌人能力 | 实际包含该敌人的关卡 | 至少一次完整前摇、执行、命中或躲避、恢复循环 |
| Boss | 对应 Boss 关卡 | Boss 出场、技能循环、阶段变化、死亡和结算 |
| 关卡配置 / 障碍 | 每个被修改关卡 | 实际移动、射击、碰撞、波次推进；结构性改动需完整通关 |
| 强化系统 | 战斗 + CardSelection | 掉落/测试前置、拾取、冻结、选择、恢复、效果和叠加 |
| 暂停 / 挂起 | 战斗 + HUD + 主菜单 | ESC 暂停、恢复、返回主页、继续原局、新局丢弃旧局 |
| 存档 / 设置 | 设置 + 主菜单 + 页面刷新 | 修改、持久化、迁移/兜底、清除范围与刷新后结果 |
| 图鉴 / 武器库 | 对应资料场景 | 逐项切换/悬停、预览、文本、返回路径和画面边界 |
| 音频 | 主菜单 + 战斗 + 暂停/结算 | 首次手势解锁、事件触发、循环、音量和场景切换 |
| 性能 / 对象池 | 无尽模式或指定压力场景 | 规定时长、活跃对象数、帧时间、内存趋势和回收后状态 |

通用系统改动至少取三个哨兵场景：第一关、一个受影响的 Boss 关卡、无尽模式。只改某个局部场景时，不需要机械地重跑全部十关。

## 12. 单次 Agent 实景测试标准流程

### 12.1 准备

1. 阅读适用规则、需求、改动差异、相关场景和上下游调用链。
2. 写出本次最低层级、用例、预期结果和明确不测的内容。
3. 通过 V0 只读调查核对环境、依赖、工具、端口、配置、数据和预计副作用。
4. 向用户提交第 3 章规定的“测试前提条件确认单”，然后停止操作并等待明确批准。
5. 只执行用户已批准的环境准备；如果只获准安装依赖，安装完成后再次咨询测试授权。
6. 检查现有工作区和进程，不覆盖用户改动，不停止用户已经运行的服务器。
7. 逐项确认 `src/config/testing.ts` 中 `TESTING_FLAGS` 全部开关的实际值（当前为 `unlockAllWeapons` 与 `enhancementDropChance`），报告中必须记录。
8. 使用独立标签页或隔离浏览器上下文。涉及存档时使用专用测试数据，不污染用户正常浏览器资料。

### 12.2 启动与监听

1. 再次确认当前动作仍在用户批准范围内；未批准或前提已变化时返回 12.1，不得启动。
2. 复用已运行且来源明确的开发服务器；否则使用已获准的项目 `npm run dev` 启动，并记录实际 URL 和端口。
3. 在导航前注册控制台错误、未捕获异常、失败请求和关键响应监听。
4. 使用桌面视口执行基线验收。项目逻辑画布为 `1280 x 720`；响应式或高分屏改动应增加对应视口/DPR 用例。
5. 等待 Canvas 可见、`window.__GAME__` 就绪且目标场景 active，不用固定睡眠时间代替状态等待。
6. 保存冷启动基线截图和初始状态探针。

### 12.3 执行

1. 优先从主菜单走真实路径；只有与导航无关的定向机制才允许直接启动目标场景。
2. 每个关键步骤按“输入 -> 可见反馈 -> 状态探针”顺序记录。
3. 键盘使用真实按下/抬起，鼠标使用真实移动/按下/抬起，持续输入必须有明确时长。
4. 战斗测试至少包含移动和瞄准，避免只在静止角色上验证射击。
5. 碰撞、掉落、暂停、Boss 技能和结算必须观察完整状态变化，不只检查对象存在。
6. 完整试玩持续到预定终点，或在首次阻塞缺陷出现时停止并保留证据。

### 12.4 失败处理

1. 立即保存截图、控制台、失败请求、状态探针和最后一组输入。
2. 从干净场景或新上下文复跑一次相同步骤。
3. 将结果分类为产品缺陷、测试脚本缺陷、环境阻塞或不稳定问题。
4. 产品缺陷未修复前不得把该用例写成通过；环境阻塞不得写成产品失败。

### 12.5 清理

1. 释放所有按键和鼠标按钮，关闭 Agent 自己创建且不再需要的标签页/上下文。
2. 只停止 Agent 本次启动且用户不再需要的服务器，不停止已有进程。
3. 不删除任务开始前已存在的 `.chrome-debug`、截图、日志或用户资料。
4. 临时证据使用唯一的 `.debug-<task>-<case>` 名称。证据是结论可复核的依据（核心原则 5），汇报完成后不得自行删除：必须在结果记录中列出每项证据的文件名与存放位置，由用户决定何时清理或是否纳入版本库。
5. 测试中临时修改的测试开关必须恢复到执行前的准确值，并检查差异。

## 13. 通过标准与结论等级

每个用例只有同时满足其全部客观标准才算通过：

1. 未出现与用例相关的未捕获异常、`console.error` 或关键资源失败。
2. 目标输入确实经过玩家路径发送。
3. 目标场景、公开状态和画面结果一致。
4. 必需截图/视频/trace 和状态探针齐全。
5. 重复进入、退出或第二次执行属于需求范围时，没有监听器、对象或状态残留。

任务结论使用以下等级：

| 结论 | 使用条件 |
| --- | --- |
| 通过 | 任务要求的全部层级和用例都已执行并满足标准 |
| 客观通过，主观待验 | V4/V5 客观项通过，但 V6 手感、听感或外部试玩仍未完成 |
| 部分通过 | 部分用例通过，仍有明确未覆盖或失败项；必须列出范围 |
| 失败 | 已稳定复现产品行为不符合通过标准 |
| 未执行 | 未获得授权、未安排或当前任务不要求执行 |
| 受阻 | 环境/工具问题使计划内验证无法完成，且已记录阻塞点 |

禁止使用以下替代性结论：

1. “测试应该能过”“看代码没问题”“截图看起来正常”。
2. 用“控制台无异常”代替交互、视觉或完整关卡通过。
3. 用“成功跳进 GameScene”代替主菜单、解锁或通关流程通过。
4. 用 headless 冒烟代替有头浏览器观感、音频听感或真人手感。
5. 用一次状态注入后的机制成功代替正式掉率、经济或完整关卡通过。

## 14. 结果记录模板

```md
# 测试结果：<任务/改动>

- 日期：YYYY-MM-DD
- 代码状态：<提交号或工作区差异摘要>
- 执行者：<Agent/人工>
- 环境：<操作系统、浏览器及版本、视口、DPR、URL>
- 测试开关：<逐项列出 `src/config/testing.ts` 中 `TESTING_FLAGS` 全部开关的实际值>
- 前提条件确认：<确认时间、已说明的依赖/工具/副作用>
- 授权范围：<用户明确同意的命令和浏览器操作>

## 层级与命令

- V0：<已完成/未完成>
- V1：<命令、退出码、通过项数；或未执行原因>
- V2：<命令、退出码；或未执行原因>
- V3-V6：<实际执行层级>

## 用例

| 用例 | 前置状态 | 真实操作 | 预期 | 实际 | 证据 | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| ... | ... | ... | ... | ... | ... | 通过/失败/受阻 |

## 运行时证据

- 未捕获异常：
- console.error：
- 失败请求：
- 场景/状态探针：
- 截图、视频或 trace（含存放路径）：

## 总结

- 总结结论：通过 / 客观通过，主观待验 / 部分通过 / 失败 / 未执行 / 受阻
- 未覆盖项：
- 剩余风险：
- 建议下一步：
```

## 15. 当前项目基线

1. 自动化入口：`npm test`，当前测试位于 `tests/`，使用 Vitest。
2. 类型入口：`npm run typecheck`。
3. 构建入口：`npm run build`，会先执行 TypeScript 检查再由 Vite 构建。
4. 开发入口：`npm run dev`，默认端口 `5173`。
5. 生产预览入口：先完成获准的 `npm run build`，再执行 `npm run preview`。
6. 游戏逻辑尺寸：`1280 x 720`，运行时可能使用 `1x` 或 `2x` 渲染缓冲区。
7. 开发探针：`window.__GAME__`，仅开发模式存在。
8. 当前没有正式 Playwright E2E 配置；现有浏览器冒烟方案见 `docs/TESTING.md`。
9. 当前 `src/config/testing.ts` 包含测试配发与掉率覆盖开关，任何实景结果都必须记录其实际值。

V1 选择受影响测试时参考以下映射（随测试文件增减更新，以 `tests/` 内实际 import 为准）：

| 测试文件 | 覆盖对象 |
| --- | --- |
| `tests/config-integrity.test.ts` | 武器、感染体、关卡、道具、键位、图鉴、武器库配置的交叉引用与 `config/validate` 校验 |
| `tests/levels.test.ts` | `config/levels.ts` 关卡与波次约束、战场主题映射 |
| `tests/enhancements.test.ts` | `config/enhancements.ts` 强化卡、`systems/EnhancementManager`、`config/testing.ts` 掉率覆盖 |
| `tests/combat-alerts.test.ts` | `config/combatAlerts.ts` 战斗警报契约 |
| `tests/combat-rules.test.ts` | `systems/EnemyAbilityRules.ts` 敌人能力启动判定 |
| `tests/monster-library.test.ts` | `config/monsterLibrary.ts` 图鉴档案与掉落说明 |
| `tests/weapon-manager.test.ts` | `systems/WeaponManager.ts` 武器与换弹生命周期 |
| `tests/weapon-loadout.test.ts` | `systems/GameState.ts` 初始配发、`systems/ProjectileImpact` 命中判定 |
| `tests/ammo-supply-rules.test.ts` | `systems/AmmoSupplyRules.ts` 缺口权重、低弹保底、高库存抑制和可复现随机源 |
| `tests/save-manager.test.ts` | `systems/SaveManager.ts` 存档归一化与迁移 |
| `tests/spatial-hash.test.ts` | `utils/SpatialHash.ts` 空间哈希 |
| `tests/geometry.test.ts` | `utils/geometry.ts` 旋转 AABB |

规范后续随正式 E2E 接入、支持浏览器矩阵、CI 门槛和公开试玩要求一起更新；在这些能力真正落地前，不得把计划中的工具或覆盖范围写成已经具备。

## 16. 变更记录

| 版本 | 日期 | 变更摘要 |
| --- | --- | --- |
| 1.0 | 2026-08-10 | 初版：V0-V6 分层、决策树、授权门禁、Canvas 专项规则、覆盖矩阵、流程与结论口径 |
| 1.1 | 2026-08-10 | 评审修订：上层规则指向改为实际存在的文件；补 L1→V1+V2 映射与快速使用路径；决策树补类型契约升级与 typecheck 缺口说明；新增 3.4 仅 V1 轻量确认与第 15 章测试覆盖映射表；探针补 GameScene 未启动前置条件；证据改为汇报后保留、由用户决定清理；模板与流程改为逐项记录 `TESTING_FLAGS`；删除外部工具指南链接 |
