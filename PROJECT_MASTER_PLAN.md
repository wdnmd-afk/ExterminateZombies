# ExterminateZombies 项目总规划

> 文档状态：讨论稿 v0.1  
> 建立日期：2026-07-29  
> 当前用途：项目方向、设计决策、阶段路线和执行治理的唯一根入口  
> 当前 Goal：尚未创建；待首轮方向决策锁定后再建立

---

## 1. 文档定位

### 1.1 目标

本文档用于把 `ExterminateZombies` 从“已有可玩原型和多份阶段文档”推进为“方向统一、边界明确、能够按 Goal 持续交付的完整项目”。

它主要回答：

1. 项目最终要做成什么。
2. 当前仓库真实完成到了哪里。
3. 玩法、怪物、武器、关卡、美术、音频、工程等方向分别需要规划什么。
4. 哪些内容已经确认，哪些仍需讨论，哪些暂时不做。
5. 后续如何使用“Goal + 设计文档 + 执行文档”推进，避免文档和代码再次漂移。

### 1.2 文档层级

后续文档按以下层级维护：

1. 根目录 `PROJECT_MASTER_PLAN.md`  
   项目总纲、决策记录、阶段状态和跨方向依赖的唯一入口。
2. `docs/design/`  
   后续按领域建立玩法、怪物、武器、关卡、美术、音频、UI、数值等设计文档。
3. `docs/execution/`  
   每个复杂 Goal 的实施文档，记录目标、范围、步骤、风险、验证与执行结果。
4. 现有 `docs/*.md`  
   暂时保留为历史方案和设计输入；其中内容只有经过本文件确认后，才视为当前决策。

### 1.3 事实优先级

当文档之间冲突时，按以下顺序判断：

1. 已通过验证的当前代码行为。
2. 本文件中标记为“已确认”的决策。
3. 对应领域设计文档。
4. 当前 Goal 的执行文档。
5. 历史计划、旧截图和未收尾草稿。

代码只能证明“现在做了什么”，不能自动证明“方向就是正确的”；方向仍以已确认的设计决策为准。

### 1.4 状态标签

| 标签 | 含义 |
| --- | --- |
| `[事实]` | 已从当前代码、配置或资源中确认 |
| `[已验证]` | 已执行对应验证并得到通过结果 |
| `[实施中]` | 已开始修改，但尚未完成全部验收 |
| `[建议]` | 推荐方案，尚待用户确认 |
| `[待确认]` | 会影响后续设计或范围，不能擅自决定 |
| `[搁置]` | 当前阶段明确不做，但保留未来可能性 |
| `[废弃]` | 已明确不再采用 |

### 1.5 本轮范围

本轮只完成：

1. 深入预读项目架构、玩法调用链、配置、资源、历史文档和验证能力。
2. 建立根目录总规划文档。
3. 记录当前真实基线、规划方向、阶段路线、风险和讨论顺序。
4. 定义后续 Goal 与设计/执行文档的协作方式。

本轮不包含：

1. 不修改玩法、数值、场景或资源实现。
2. 不为尚未确认的方向创建实现 Goal。
3. 不执行全量构建。
4. 不把缺少浏览器实测的内容标记为完整验收通过。

---

## 2. 项目当前定义

### 2.1 一句话定义

`[建议]` 一款面向桌面浏览器的单人俯视角动作射击游戏：玩家在短时高压战斗中移动拉扯、识别敌群威胁、临场获得武器，并利用可连锁的场景物制造生存空间。

### 2.2 当前产品形态

`[事实]` 当前版本具备：

1. 固定画面的 `1280 × 720` 逻辑战场。
2. 键盘移动、鼠标瞄准与射击。
3. 三个固定关卡和一个无尽模式。
4. 波次战斗、掉落拾取、关卡解锁、失败重开与通关结算。
5. 武器库、怪物图鉴、键位设置和本地存档。
6. 像素角色与感染体、战报式菜单和 HUD。

### 2.3 推荐的体验支柱

以下支柱需要在讨论后正式锁定：

1. **移动与枪感**：移动、瞄准、开火、命中、换弹和击杀都必须清楚且有区分。
2. **敌群判断**：玩家处理的不是单纯血量总和，而是不同威胁的优先级。
3. **环境战术**：油桶、粉尘、地雷、障碍和关卡布局必须改变决策。
4. **短局递进**：几秒内进入战斗，数分钟内形成压力，单局结束后愿意重开。
5. **统一像素风格**：世界实体与视觉资产统一像素规范；现有战报式 UI 排版按 D-003 保留，不引入美漫矢量资产。

### 2.4 建议的近期非目标

在核心体验和垂直切片通过前，建议不做：

1. 账号系统、云存档和在线排行榜。
2. 联机合作或联机对战。
3. 移动端触控适配。
4. 大型开放世界、复杂剧情 RPG 或重刷装成长。
5. 为未来可能性提前引入后端。
6. 无验证地继续扩充大量同质武器、怪物或关卡。

---

## 3. 深度预读后的真实基线

本节最初记录 2026-07-29 的仓库快照，并于 2026-07-30 同步半成品修复进展。当前可靠性、敌人能力、音频、场景主题和自动化测试已完成代码接线；浏览器级运行验收仍需补齐。

### 3.1 技术栈

| 类别 | 当前实现 |
| --- | --- |
| 游戏引擎 | Phaser 3.90.0（声明范围 `^3.80.1`） |
| 语言 | TypeScript 5.9.3（声明范围 `^5.5.4`），`strict`、未使用变量检查开启 |
| 构建工具 | Vite 5.4.21 |
| 物理 | Phaser Arcade Physics |
| 持久化 | 版本化 `localStorage`，通过 `SaveManager` 校验、迁移和重置 |
| 后端 | 无，当前为纯静态单机项目 |
| 运行平台 | 桌面浏览器，键盘 + 鼠标 |
| 渲染 | 逻辑分辨率 1280 × 720；按屏幕选择 1×/2× 缓冲区 |
| 自动化测试 | Vitest：7 个测试文件、26 项用例；正式端到端测试尚未接入 |
| 音频 | Phaser Web Audio 加载开放授权音效与菜单/战斗循环，支持三档音量、变体、限频和空间声像 |

### 3.2 场景流

`[事实]` 当前场景链路为：

```text
Boot
  -> Preload
  -> MainMenu
       -> Game + HUD
       -> WeaponLibrary
       -> MonsterLibrary
       -> Settings
  -> GameOver / LevelClear
```

### 3.3 玩法内容盘点

| 方向 | 当前数量/状态 | 判断 |
| --- | --- | --- |
| 固定关卡 | 3 个可进入关卡 | 已有主题化程序地面、边界、障碍和波次；正式位图环境资产与最新浏览器验收仍缺失 |
| 无尽模式 | 1 套程序化波次 | 已具备递增敌人池、周期 Boss、场景物上限和空间哈希邻近查询 |
| 正式武器 | 4 把 | 沙漠之鹰、MP5、M4A1、SPAS-12 |
| 测试配发武器 | 4 把 | AK-47、Barrett M82、RPG-7、M79 已具备战斗配置并随新局配发，尚未进入正式掉落和平衡 |
| 感染体配置 | 16 类 | 14 类常规感染体 + 2 类 Boss |
| 感染体基础行为 | 追击、接触、死亡爆炸、冲刺、远程投射 | 特殊攻击已统一采用前摇、执行、恢复和冷却；部分条目仍是近战视觉/数值亚种 |
| Boss | 2 类 | 坦克 Boss 有震荡范围技能，爆破 Boss 有目标区域轰炸与死亡爆炸 |
| 场景物/道具 | 3 类 | 油桶、面粉桶、地雷 |
| 障碍物 | 3 类外观 | 集装箱、废车、路障 |
| 武器美术 | 8 把已完成 | 逐把对应真实型号的透明像素贴图，玩家补通用持枪手臂层，枪口出弹点对齐枪管，实机与武器库共用同一套素材；缺分武器持枪动画与有头浏览器观感验收 |
| 存档项 | 5 类 | 版本、键位、音量、已解锁关卡、无尽最佳波次；支持旧数据归一化和清除进度 |
| 音频 | 已接线 | 46 个运行时音频文件、八把武器/战斗/UI 事件、菜单/战斗循环和音量设置已接线；Chrome 解码与关卡触发已验证，待主观听感及多浏览器验收 |
| 实际战斗场景 | 实施中 | 已移除共享网格并建立关卡主题地面/边界；正式位图环境资产和截图验收仍未完成 |
| 可玩性测试 | 未完成 | 尚无完整关卡试玩、速度调优、难度记录和外部试玩结论 |

### 3.4 当前感染体扩展状态

`[实施中]` 当前代码已经完成以下接线：

1. `ZOMBIES` 和怪物图鉴扩为 16 项。
2. 三关固定波次已经加入新增类型。
3. 无尽模式使用解锁波次和权重表生成敌群。
4. 多套 Curt、Cabbit、Reemax、CornerLord、SpriteAttack 素材已经映射。
5. 四方向素材和俯视旋转帧条均有对应运行时结构。
6. 怪物图鉴已经改为双列索引。
7. `[废弃]` 原记录「`npm run typecheck` 已在本轮重新执行并通过」。2026-07-30 复核时该命令实际失败：`tests/weapon-loadout.test.ts` 使用 `node:fs`、`node:url` 和 `Buffer`，但仓库未安装 `@types/node`，而 `tsconfig.json` 的 `include` 覆盖了 `tests`，共 3 个错误。因 `build` 脚本为 `tsc --noEmit && vite build`，`npm run build` 同时处于失败状态。
8. runner/feral/crawler/stalker 已接入冲刺能力，lurker/rotting/oddity 已接入远程投射。
9. 两个 Boss 已接入独立范围技能、危险预警和 HUD 生命条。
10. `npm test` 已接入并通过 26 项配置与纯逻辑测试。
11. 全武器测试开关当前开启，新局拥有 8 把武器；RPG-7/M79 已接入单次命中爆炸。
12. 8 把武器已接入逐把对应真实型号的像素贴图，玩家补持枪手臂层，枪口出弹点与枪管末端对齐，武器库与实机共用同一套贴图；详见 `docs/execution/2026-07-30-real-weapon-models.md`。

仍未据本轮独立确认：

1. 16 类动画方向、缩放、锚点和旋转偏移是否全部正确。
2. 三关和无尽模式能否完整跑通且无运行时异常。
3. 双列图鉴在真实 `1280 × 720` 画布中是否无溢出、遮挡。
4. 新敌人能力加入后，前摇、并发、难度、掉落经济和性能是否仍合理。
5. `docs/execution/2026-07-29-zombie-roster-expansion.md` 已填写执行结果，但明确保留浏览器实测待办。
6. 武器贴图只在 headless 浏览器验收，尚缺有头浏览器观感与斜向持枪贴合度确认。

### 3.5 当前架构

```text
config
  weapons / zombies / items / levels / keybinds / libraries
      |
      v
systems
  Input / Weapon / Item / Wave / EnemyAbility / AreaEffect
  Battlefield / Sound / Save / Display / Assets
      |
      v
entities
  Player / Zombie / Bullet / EnemyProjectile / Pickup / Prop / Obstacle
      |
      v
GameScene 负责装配、碰撞、生命周期、奖励与场景流转
      |
      +--> HUDScene 通过事件读取 GameState
```

架构优势：

1. 内容配置和运行系统已有基本分层。
2. `WeaponId`、`ZombieId` 等类型正在逐步收紧。
3. 高频对象已使用对象池。
4. 存档、显示和资源映射有独立入口。
5. 图鉴尽量从真实配置派生，减少重复数据。
6. 配置在 Boot 阶段执行运行时完整性校验。
7. 存档、旋转碰撞、空间哈希和换弹生命周期已有自动化测试。

架构压力：

1. `GameScene.ts` 已超过 700 行，同时承担装配、碰撞、掉落、结算、特效和流程控制。
2. `Zombie` 仍持有能力状态机，但具体执行已收口到 `EnemyAbilitySystem`；后续扩展需要继续防止实体类膨胀。
3. 空间哈希已替代全量分离遍历，但仍需在 50/100/150 活跃敌人下实测帧率。
4. 配置完整性已有运行时校验和测试，但仍缺平衡统计脚本。
5. 存档已有版本、迁移、校验和重置；仍需真实旧浏览器数据迁移验证。

### 3.6 美术与资源基线

`[事实]` 当前 `src/assets` 约 8.2 MB、154 个文件，包含：

1. 已接入的像素玩家、感染体和武器图标。
2. 尚未完全接入的候选角色、感染体、枪械和三维角色资源。
3. 原始 ZIP、派生 PNG、源文件、许可证和署名记录。
4. CC0、CC-BY 3.0、CC-BY 4.0、OGA-BY 等不同授权来源。

当前优点：

1. 多数外部素材有来源、作者、许可证和哈希记录。
2. 原始素材与处理后素材基本分开保存。
3. 有可复现的 GIF 转 PNG 帧条脚本，以及可复现的武器裁剪脚本 `scripts/process_weapon_assets.py`。
4. 8 把武器均使用逐把对应真实型号的透明贴图，实机与武器库共用同一套素材，不再出现素材表烘入的型号名文字与浅灰底。

当前问题：

1. 像素密度、视角和作者风格不完全统一。
2. 玩家使用「身体 + 通用持枪手臂 + 武器贴图」三层组合。8 把武器共用同一手臂姿势，长枪缺少支撑手，尚未具备分武器持枪动画。
3. 三关地面、道具和拾取物已有更明确程序化表现，但正式位图环境素材仍缺失。
4. 旧资源 README 中部分“尚未接入”描述已经过时。
5. 游戏内尚无正式 Credits/许可证页面。

### 3.7 工程与仓库卫生

`[事实]` 当前工作目录状态：

1. Git 元数据和根目录 `.gitignore` 当前均存在，可读取工作区差异。
2. 根目录仍存在调试截图、Chrome 用户资料、Vite 日志和临时脚本。
3. `node_modules` 位于工作目录内。
4. README 已存在并同步当前功能；版本发布说明和部署清单仍缺失。

这些问题不会直接改变玩法，但会降低可追溯性、协作安全和发布质量，必须进入基线固化阶段。

### 3.8 现有文档漂移

1. `docs/PLAN.md` 是早期架构方案，许多“占位图形”描述已过时。
2. `docs/Long_Term_Goals.md` 和长期路线图仍把正式角色素材视为未来任务。
3. 原 `docs/Comic_Art_Style_Plan.md` 的美漫矢量/SVG 方向与像素实现冲突，已于 2026-08-05 按用户决策删除。
4. 多份资源 README 仍写着“尚未接入”，与当前代码不一致。
5. 感染体扩展执行文档已经补充结果，但浏览器实测完成后还需要再次同步最终状态。

结论：旧文档有价值，但不能继续分别充当事实来源。本文件建立后，后续先更新总纲，再更新领域文档。

### 3.9 用户确认的当前判断

`[已确认]`

1. 当前实际游戏场景尚未完成，不能因为存在 `GameScene`、关卡配置和场景物代码就视为场景已交付。
2. 当前可玩性尚未经过完整测试，不能声称关卡节奏、战斗手感或难度已经成立。
3. 当前感染体整体移动过快，需要在真实运行环境中记录并下调速度曲线。
4. 后续感染体必须拥有不同攻击方式，至少包括远程攻击型和闪现/位移型。
5. 最终闯关内容不少于 30 关，但当前不批量制作关卡；先完成场景、战斗和可玩性细节。
6. 场景需要达到精细化标准，不能继续停留在网格背景加少量程序化障碍物的状态。

---

## 4. 必须先讨论并锁定的总方向

### 4.1 D-001：最终产品形态

`[已确认]` 项目主体是长期关卡闯关：

1. 最终闯关关卡不少于 30 关。
2. 当前阶段不直接扩到 30 关，先完成真实场景、战斗细节、怪物机制和可玩性验证。
3. 现有无尽模式可以保留为副模式，但优先级低于闯关主线。
4. 是否加入局内构筑、随机升级或局外解锁，继续作为独立决策，不与“30 关主线”绑定。
5. 30 关必须通过可复用的关卡生产规范分批实现，不能复制同一竞技场只替换波次数值。

### 4.2 D-002：单局长度与节奏

`[建议]`

1. 首次进入战斗：5 秒以内。
2. 教学型关卡：8-10 分钟。
3. 标准关卡：10-15 分钟。
4. 无尽模式：3 分钟进入明显压力，10 分钟进入高风险，15 分钟后成为极限挑战。
5. 死亡后两次操作以内重开。

需要通过真实试玩记录校准，不能只按波次数量推算。

### 4.3 D-003：美术方向

`[已确认]`（2026-08-05 用户决策）项目美术规范为**像素风格**：

1. 玩家、怪物、武器、场景物、地面与战场特效统一使用像素风格与一致的像素尺度。
2. 美漫矢量/SVG 方向废弃，`docs/Comic_Art_Style_Plan.md` 已删除（历史版本可从 git 追溯）。
3. 不把不同作者资源直接并排使用；先建立色板、轮廓、比例和缩放白名单。
4. 现有菜单/HUD 的战报式排版按当前实现保留；后续 UI 视觉制作不得引入与像素战场冲突的美漫矢量资产，如需调整 UI 风格另行决策。

### 4.4 D-004：感染体数量与“种类”定义

当前 16 类已经在配置层成立，但“16 个数值组合”不等于“16 种玩法”。

`[已确认]` 感染体需要通过攻击方式和行为改变玩家决策，不能只依赖移动速度、生命和伤害区分。

最低行为范围包括：

1. 近战追击与接触攻击。
2. 远程攻击：需要投射物、攻击前摇、弹道、命中和躲避窗口。
3. 闪现/位移攻击：需要起点预警、落点预警、安全距离、冷却和反制窗口。
4. 死亡爆炸或区域危险。
5. Boss 的阶段技能和组合攻击。

仍建议把感染体分成两个概念：

1. **视觉亚种**：外观、体型、基础数值不同，可复用同一行为。
2. **战斗角色**：会改变玩家决策，必须拥有独立行为、预警或战场作用。

现有 16 类可以作为视觉与数值池，后续必须逐类登记攻击模式和战斗角色。没有独立机制的条目只能算视觉亚种。

### 4.5 D-005：地图规模

`[建议]` 近期保持固定单屏竞技场，不引入滚动大地图：

1. 降低寻路、镜头、关卡生产和性能成本。
2. 把差异集中在布局、目标、敌群、场景机关与 Boss。
3. 等垂直切片证明单屏玩法深度不足，再讨论多房间或滚动地图。

### 4.6 D-006：最终内容规模

`[已确认]` 正式闯关主线最终不少于 30 关。

`[建议]` 采用“先切片、再分批扩展”的结构：

| 内容 | 垂直切片 | 第一批正式内容 | 最终项目目标 |
| --- | ---: | ---: | ---: |
| 正式关卡 | 1 | 5 | 30+ |
| 可用武器 | 4 | 待确认 | 待确认 |
| 清晰战斗角色 | 至少包含近战、远程、闪现 | 待确认 | 待确认 |
| Boss | 1 | 待确认 | 待确认 |
| 战术场景物 | 3 | 待确认 | 待确认 |

执行约束：

1. 第一关垂直切片未通过前，不开始批量制作第 2-30 关。
2. 每关必须有明确主题、空间布局、敌人组合、环境战术和节奏记忆点。
3. 关卡按批次验收；前一批未达到质量门槛，不开启下一批。
4. 当前三个关卡配置不自动计入“已完成正式关卡”。

---

## 5. 各规划方向

## 5.1 核心玩法与操作

### 当前状态

1. WASD 移动、鼠标瞄准、左键射击。
2. 换弹、切枪、放置/切换道具、暂停均已接入。
3. 基础循环为“清波 -> 拾取 -> 下一波 -> Boss/结算”。

### 需要设计

1. 玩家每 10 秒、每 1 分钟和每一局分别在做什么决策。
2. 移动速度、转向、受击无敌帧和碰撞宽容度。
3. 手动换弹、自动换弹、空弹提示和切枪取消规则。
4. 局内是否存在升级、三选一、商店或波间补给。
5. 失败原因是否可读，重开是否足够快。

### 推荐原则

核心循环应表达为：

```text
识别威胁
  -> 移动拉扯并规划退路
  -> 选择武器/目标
  -> 利用场景制造窗口
  -> 拾取资源并重组策略
  -> 进入更高压力波次
```

任何新系统如果不能改变这条循环中的至少一个决策，不进入近期范围。

### 后续文档

`docs/design/GAMEPLAY.md`

---

## 5.2 战斗手感

### 当前状态

已有枪口闪光、命中火花、受击闪烁、屏幕震动、爆炸冲击和拟声词反馈，但没有声音层，反馈仍主要由程序图形承担。

### 需要设计

1. 每把武器的后坐、枪口、弹道、命中、击杀和换弹反馈。
2. 轻敌、重敌、装甲敌、爆炸敌的命中差异。
3. 屏幕震动、闪光、粒子数量和持续时间的统一等级。
4. 击杀停顿、击退或伤害数字是否需要。
5. 高密度战斗时如何防止视觉噪声。

### 验收方向

玩家不看 HUD 也能判断：

1. 是否成功开火。
2. 是否命中以及命中对象是否耐打。
3. 是否正在换弹或已经空弹。
4. 爆炸范围是否危险。
5. 自己因什么受到伤害。

---

## 5.3 武器设计

### 当前状态

| 武器 | 当前定位 | 主要问题 |
| --- | --- | --- |
| 沙漠之鹰 | 无限备用弹、稳定保底 | 需要确认是否真的符合“大口径手枪”手感 |
| MP5 | 高射速近中距离压制 | 与 M4A1 的资源和射程边界需实测 |
| M4A1 | 高伤、穿透、中远距离 | 当前射速也很高，可能覆盖其他职责 |
| SPAS-12 | 近距离多弹丸爆发 | 需验证弹药稀缺与近身风险是否匹配 |

### 每把正式武器必须定义

1. 战斗职责。
2. 有效距离和失效场景。
3. 爆发 DPS、持续 DPS、单次击杀阈值。
4. 弹药效率、换弹窗口和资源来源。
5. 对单体、群体、Boss、场景物的相对优势。
6. 操作技巧和主要风险。
7. 独立的视觉与声音签名。
8. 获取方式与出现时机。

### 扩展建议

1. 垂直切片仍优先把 4 把正式武器做出明确差异；另外 4 把当前只作为测试配发开放。
2. 后续优先增加“新职责”，不是同类 DPS 升级。
3. AK-47 已有高威力自动武器测试配置，后坐规则仍未实现。
4. Barrett 已有低射速、高穿透测试配置，待验证精英处理边界。
5. RPG/M79 已完成直线爆炸弹、玩家误伤、射程引爆和场景连锁测试接线；正式弹道与入池决定仍待确认。
6. 测试配置不自动代表正式内容；逐把完成手感和平衡验收后再决定入池。
7. 视觉签名已达成：8 把武器均为对应真实型号的独立贴图，玩家持枪姿态、枪口位置和武器库预览一致；声音签名已映射到开放授权枪声样本并通过速率、音量和变体区分，仍待浏览器主观听感验收。

### 后续文档

`docs/design/WEAPONS.md`

---

## 5.4 怪物与 AI 设计

### 当前状态

绝大多数感染体共享“直线追击 + 分离力 + 接触攻击”，差异来自生命、速度、体型、伤害、攻击间隔、掉落和少量死亡爆炸。

当前玩家移动速度为 `240 px/s`；感染体配置速度范围约为 `26-125 px/s`，其中 `runner`、`feral`、`crawler` 等高速类型会叠加敌群包围、障碍物和转向压力。用户已经确认当前实际感受偏快，因此现有数值不能视为平衡基线。

### 已确认的速度调整原则

1. 先在真实场景记录玩家、普通怪、高速怪和重型怪的追击表现。
2. 下调当前感染体整体压力，尤其先检查 `runner`、`feral`、`crawler`。
3. 建立“缓慢、标准、快速、爆发位移”四种速度层级。
4. 闪现和冲刺属于技能位移，不与持续高速移动叠加。
5. 普通敌人需要给玩家稳定拉扯空间；高速敌人必须低耐久、低数量或有明显前摇。
6. 调速后必须重新验证障碍绕行、敌群分离、玩家受击频率和无尽模式压力。
7. 在完整试玩前不直接把某个统一倍率写成最终答案。

### 推荐战斗角色

1. **基础压力型**：持续压缩空间。
2. **高速包夹型**：迫使快速转火和横向移动。
3. **重装火力税型**：长期占据弹药和输出窗口。
4. **死亡危险型**：改变最后一击的位置选择。
5. **远程压制型**：迫使玩家离开固定拉扯路线。
6. **区域控制型**：制造毒区、减速区或封路。
7. **支援/指挥型**：强化、召集或保护其他敌人。
8. **伏击/突进型**：通过前摇明确的冲刺改变节奏。

### 行为系统最低设计

所有具备特殊攻击的感染体，应按统一阶段执行：

```text
接近/选位
  -> 攻击前摇
  -> 锁定目标或落点
  -> 执行攻击
  -> 恢复窗口
  -> 重新接近/选位
```

禁止把远程、闪现、冲锋等能力直接写成无前摇的瞬时伤害。

#### 远程攻击型

正式实现至少需要：

1. 最小和最大攻击距离。
2. 射击前摇与明显姿态/颜色/音效提示。
3. 可躲避的投射物速度和尺寸。
4. 投射物与障碍物、玩家、场景物的碰撞规则。
5. 攻击冷却和攻击后的恢复窗口。
6. 玩家贴身后采用撤退、近战或无法攻击中的一种明确规则。
7. 多只远程怪同时出现时的数量上限或错峰机制。

#### 闪现/位移攻击型

正式实现至少需要：

1. 闪现触发距离和冷却。
2. 消失前的明显前摇。
3. 目标落点选择规则。
4. 禁止落在玩家碰撞体、障碍物或不可达区域内。
5. 落点提前显示危险标记。
6. 出现后保留短暂反应窗口，不能落地即必中。
7. 闪现能否被伤害、控制或场景效果打断。
8. 多只闪现怪的并发上限，避免不可读的连续瞬移。

### 分阶段建议

1. 保留现有 16 类配置，先作为内容池。
2. 从中选择 6-8 类，赋予清晰战斗角色。
3. 同行为的其余类型作为不同关卡的视觉亚种或难度变体。
4. 在配置中引入明确的行为 ID 或能力列表前，先设计行为协议，不直接在 `Zombie.ts` 堆 ID 判断。
5. 每个新行为必须有前摇、执行、恢复和中断规则。

### 性能与寻路

1. 当前直线追击适合开放单屏地图。
2. 障碍增多后，需要明确“沿墙滑行是否足够”。
3. 只有出现可复现卡死时再引入路径网格或流场。
4. 当活跃感染体达到约 80-120 且帧率下降时，评估空间哈希替代两层分离遍历。

### 后续文档

`docs/design/MONSTERS.md`

---

## 5.5 Boss 设计

### 当前状态

当前有 4 个 Boss，均已接入同一作者组的独立俯视像素移动帧条。四者仍复用统一实体与追击框架，每个 Boss 目前只有一类配置化特殊能力，尚无阶段变化和完整攻击/死亡动画状态机。

### 每个正式 Boss 必须具备

1. 独立轮廓或明显可读的视觉改造。
2. 至少两种不同节奏的技能。
3. 清晰前摇、危险区和恢复窗口。
4. 与关卡场景物或地形发生关系。
5. 阶段变化或压力升级。
6. 专属音效、HUD 提示和死亡表现。
7. 可学习、可躲避、可复盘，而不是只靠堆血。

### 推荐首个垂直切片

优先把 `tank_boss` 做成一场完整 Boss 战：

1. 重击或冲锋。
2. 震荡范围预警。
3. 撞击障碍或场景物后的可利用窗口。
4. 第二阶段缩短恢复时间，而不是只增加伤害。

---

## 5.6 道具、场景物与环境战术

### 当前状态

1. 油桶：爆炸 + 火焰持续伤害。
2. 面粉桶：爆炸 + 粉尘阻挡。
3. 地雷：玩家放置、近距离触发。
4. 爆炸可伤害玩家并产生连锁。

### 需要锁定

1. 玩家误伤是否始终开启。
2. 连锁爆炸是否按同帧递归，是否需要短延迟提升可读性。
3. 火、粉尘、毒、酸、电等区域效果的统一规则。
4. 场景物能否被推动、摧毁或刷新。
5. 关卡场景物是固定布局、随机布局还是混合布局。
6. 道具携带上限、切换和补给节奏。

### 扩展顺序

1. 先打磨现有三类的反馈与平衡。
2. 再增加一种不可移动掩体机关和一种主动控场道具。
3. 最后考虑门、电网、警报器、车辆等复合机关。

### 后续文档

`docs/design/ENVIRONMENT.md`

---

## 5.7 关卡、地图与波次

### 当前状态

三个关卡已不再共享固定网格：郊外、废车站和封锁城区分别具备主题化程序地面、边界、非碰撞细节、障碍物、场景物和敌群组合。当前仍缺正式位图环境资产、环境动态层和最新浏览器截图/试玩验收，因此三关仍不能标记为正式关卡。

### 精细场景最低标准

每个正式场景至少需要：

1. **独立地点主题**：地点、时间、灾变痕迹和环境叙事明确。
2. **正式地面层**：主地表、破损、污渍、血迹、轮胎印或其他局部变化，不能只使用规则网格。
3. **边界层**：墙体、围栏、建筑边缘、废墟或自然边界，明确说明为什么玩家不能离开。
4. **可碰撞主体**：掩体、车辆、集装箱、路障等外观必须与物理碰撞体基本一致。
5. **非碰撞细节层**：小型废弃物、标识、残骸、植被、线缆等，用于丰富空间但不能干扰战斗识别。
6. **交互层**：油桶、粉尘桶、地雷、机关等位置要形成战术选择。
7. **环境动态层**：克制的烟尘、火星、灯光闪烁、风动或远景动态。
8. **出生与警告层**：玩家出生点、敌人入场方向、Boss 入场和危险区域清晰可读。
9. **视觉导航**：主通路、危险死角、资源点和战术热点能从画面上快速辨认。
10. **性能与遮挡检查**：装饰不能遮挡玩家、怪物、子弹、掉落或危险预警。
11. **截图验收**：在实际 `1280 × 720` 画布检查整体构图、层级、碰撞对齐和文字遮挡。
12. **试玩验收**：场景必须支持完整打一局，证明空间布局确实产生预期玩法。

### 正式关卡模板

每关必须明确：

1. 关卡主题与视觉地点。
2. 本关教授或考验的核心技能。
3. 地图布局、主要通路和危险死角。
4. 场景物分布与预期战术。
5. 新敌人引入顺序。
6. 波次节奏：准备、积压、释放、高潮。
7. 独立目标或事件。
8. Boss/终局压力。
9. 通关奖励和下一关解锁。
10. 首通时间、失败位置和资源曲线指标。

### 推荐定位

1. 第一关：教学型，突出油桶、粉尘和基础敌人优先级。
2. 第二关：高压拉扯型，突出狭窄通路、快速敌人与重装 Boss。
3. 第三关：区域管理型，突出死亡爆炸、控场和复合敌群。

当前三关可作为原型底稿，但需要正式地面资产、节奏记录和玩法记忆点后才算正式内容。

### 30 关生产约束

`[已确认]` 最终至少 30 关，但当前冻结批量扩关。

批量生产前必须先完成：

1. 第一关精细场景垂直切片。
2. 一套可复用但不会导致同质化的场景组件库。
3. 一份正式关卡模板和验收表。
4. 怪物特殊攻击框架。
5. 武器、掉落和速度的首轮可玩性验证。
6. 关卡批次与章节结构设计。

建议按每批 5 关推进。每批验收后才能制作下一批；具体章节数量和主题仍待讨论。

### 无尽模式

无尽模式后续至少需要：

1. 阶段性敌人池。
2. 特殊事件波。
3. 补给波或风险换资源事件。
4. 精英/Boss 节点。
5. 资源压力曲线。
6. 可理解的记录与结算。

### 后续文档

`docs/design/LEVELS.md`

---

## 5.8 局内成长、解锁与复玩

### 当前状态

1. 武器和弹药只在单局内获得。
2. 局外只保存关卡解锁和无尽最佳波次。
3. 没有属性成长、天赋、成就或挑战。

### 推荐方向

1. 局外只解锁新武器、新关卡、新挑战和图鉴，不永久增加基础伤害/生命。
2. 如需构筑深度，优先加入局内的有限选择，例如每两波一次三选一。
3. 局内选择应改变打法，例如穿透、燃烧、换弹、地雷、移动，而不是统一加百分比。
4. 先完成纯战斗垂直切片，再决定是否加入局内升级系统。

### 存档要求

在增加新进度前，必须补：

1. `saveVersion`。
2. 数据校验。
3. 默认值合并。
4. 旧版本迁移。
5. 清除/重置存档入口。

---

## 5.9 数值平衡与资源经济

### 当前问题

1. 数值主要靠配置直填，缺少可复现的平衡记录。
2. 16 类感染体加入后，关卡总血量、击杀时间和掉落量已经发生明显变化。
3. 手枪无限弹保证不会软锁，但可能削弱弹药压力。
4. 多种敌人掉落武器和补给，实际经济依赖随机结果。

### 必须建立的指标

1. 各武器爆发/持续 DPS。
2. 对每类敌人的理论击杀时间和所需弹量。
3. 每波总生命、总伤害压力和理论掉落期望。
4. 玩家平均持有弹药、血量和地雷数量。
5. 首通时间、死亡波次和失败原因。
6. 无尽前 5、10、15 波的敌人数与性能。

### 实施建议

1. 先建立只读平衡脚本或表格，不急于做游戏内调试器。
2. 随机掉落测试使用可选固定种子或调试日志。
3. 每轮只调整一个主要变量组，记录调整前后结果。
4. 不以“能通关一次”作为平衡完成标准。

### 后续文档

`docs/design/BALANCE.md`

---

## 5.10 美术风格与资源生产

### 建议的美术圣经内容

1. 逻辑像素密度和推荐源帧尺寸。
2. 玩家、普通敌人、精英、Boss 的显示高度区间。
3. 最近邻采样、整数/稳定缩放规则。
4. 主色板、危险色、补给色和阵营识别。
5. 阴影、描边、受击闪白和 Boss 标识规则。
6. 地面、障碍、场景物、拾取物、弹丸和特效风格。
7. UI 的字体、边框、拟声词和动画规则（遵循 D-003 像素规范，不引入美漫矢量资产）。
8. 原始资源、处理脚本、派生资源和运行时映射的目录规范。
9. 授权、署名、哈希和 Credits 要求。

### 资源优先级

1. 玩家与当前武器表现。
2. 高频感染体与首个 Boss。
3. 地面、油桶、面粉桶、地雷和拾取物。
4. 子弹、命中、爆炸和区域效果。
5. 正式关卡障碍与背景。
6. 菜单、HUD 和结算细节。

### 约束

1. 不因已有素材而强行设计玩法。
2. 不把未接入候选资源计入正式内容。
3. 不直接混用不同像素密度和视角。
4. CC-BY/OGA-BY 资源必须进入游戏内 Credits。
5. 未使用的三维资源不进入运行时加载。

### 后续文档

`docs/design/ART_BIBLE.md`

---

## 5.11 音效与音乐

### 当前状态

已建立统一 `SoundManager`，通过 Phaser Web Audio 加载开放授权文件，覆盖八把武器、空弹、换弹、切枪、命中材质、感染体、爆炸、火焰、受伤、拾取、波次、Boss、结算和 UI，并具备菜单/战斗循环、浏览器手势解锁、按位置限频、并发限制、空间声像和主/音效/音乐音量设置。资源来源、处理与强制署名记录见 `docs/AUDIO_ASSET_REGISTRY.md`；Chrome 147 已通过资源解码、关卡触发、暂停恢复和音量验收，仍缺人工主观混音与多浏览器验收。

### 最小音频范围

1. 八把武器的独立开火声。`[资源与代码已接线，待听感验收]`
2. 命中材质、击杀和空弹。`[已接线]`
3. 换弹与切枪。`[已接线]`
4. 爆炸、火焰、粉尘、地雷。`[已接线]`
5. 玩家受伤和死亡结算。`[已接线；低血量循环仍待设计]`
6. 拾取、波次、Boss、按钮与结算。`[已接线]`
7. 一条菜单音乐和一条战斗循环。`[已接线]`

### 技术要求

1. 建立统一 `SoundManager` 或等价入口。`[已实现]`
2. 分离主音量、音乐、音效和 UI 音量。`[已实现主/音效/音乐；UI 当前归入音效总线]`
3. 处理浏览器首次用户手势解锁。`[Chrome 147 已验证]`
4. 高频命中与自动武器必须限制叠音。`[已实现按位置限频与事件并发上限]`
5. 暂停、场景切换和失焦时行为一致。`[音乐与世界循环的暂停/恢复/销毁已验证；浏览器失焦仍待验证]`

### 后续文档

`docs/design/AUDIO.md`；当前资源台账见 `docs/AUDIO_ASSET_REGISTRY.md`

---

## 5.12 UI、UX 与可访问性

### 当前状态

主菜单、HUD、暂停、设置、武器库、怪物图鉴和结算已经形成战地档案风，但仍缺统一组件层和完整可访问设置。

### 需要规划

1. 首次游玩如何在不打断战斗的情况下理解控制和目标。
2. 空弹、换弹、受伤来源、Boss 技能和危险区提示优先级。
3. 菜单、图鉴、设置和结算的键盘操作一致性。
4. 中文与英文混排的固定文案规范。
5. 不同窗口尺寸、1×/2× 渲染和全屏模式。

### 可访问性最低范围

1. 完整改键。
2. 主音量、音乐和音效音量。
3. 屏幕震动强度。
4. 闪光/高亮强度。
5. 高对比模式或至少不只依赖颜色传达危险。
6. 暂停时明确恢复与返回操作。

### 后续文档

`docs/design/UI_UX.md`

---

## 5.13 世界观、品牌与文案

`[建议]` 保持轻叙事，不建立重剧情系统：

1. 用任务简报、感染体档案、武器档案和结算战报传达世界。
2. 确定玩家身份、灾变背景和三个关卡地点之间的关系。
3. 建立中文主文案与英文军事档案标签的使用规范。
4. 确认正式中文名、英文名、版本名和 Logo 使用。
5. 剧情只服务战斗动机和关卡记忆点。

后续可建立 `docs/design/WORLD_AND_TONE.md`。

---

## 5.14 技术架构与开发工具

### 近期原则

1. 继续保持配置驱动。
2. 不为尚未确认的系统提前大重构。
3. 当 Boss、远程敌人或局内升级开始实施时，再拆行为和战斗服务。
4. `GameScene` 应逐步回到“装配与场景流程”职责。

### 建议的演进点

1. 敌人行为协议或行为组件。
2. Boss 行为系统。
3. 统一伤害事件与伤害来源。
4. 统一投射物/范围攻击接口。
5. 配置完整性校验脚本。
6. 平衡统计脚本。
7. 存档版本与迁移。
8. 可选固定随机种子。
9. 性能计数器和活跃对象统计。

### 不建议立即做

1. ECS 全量重写。
2. 替换 Phaser。
3. 引入状态管理框架。
4. 引入后端。
5. 为少量配置搭建复杂编辑器。

### 后续文档

`docs/design/TECH_ARCHITECTURE.md`

---

## 5.15 性能、平台与兼容性

### 性能预算建议

1. 主流桌面浏览器以 60 FPS 为目标。
2. 2× 渲染模式无明显长期掉帧。
3. 首屏资源和启动时间建立实际测量。
4. 无尽模式分别在 50、100、150 活跃敌人下测试。
5. 短生命周期特效、文字和掉落必须及时销毁或池化。

### 兼容范围待确认

建议首发支持：

1. 最新两个主要版本的 Chrome/Edge。
2. Firefox 当前稳定版。
3. Safari 是否支持，需在发布阶段单独确认。
4. 桌面分辨率最低建议 `1280 × 720`。

移动端当前标记为 `[搁置]`。

---

## 5.16 测试、试玩与质量门槛

### 当前验证能力

1. `npm run typecheck`。
2. Vite 开发服务器。
3. 历史 CDP 临时脚本和截图。
4. 人工测试清单。

### 需要补齐

1. 配置 ID、掉落引用、图鉴覆盖和关卡引用的自动校验。
2. 纯函数单元测试：波次分配、存档迁移、数值计算。
3. 场景启动和关键跳转的运行时冒烟。
4. 桌面视口截图回归。
5. Canvas 非空、资源加载和运行时异常检查。
6. 固定试玩记录模板。

### 单项完成定义

一个 Goal 只有同时满足以下条件才可标记完成：

1. 目标行为已经实现。
2. 相关配置、UI、存档和调用链一致。
3. 最小必要类型/静态检查通过。
4. 受影响场景完成运行时验证。
5. 执行文档填写实际结果和剩余风险。
6. 本文件中的阶段状态已同步。

### 构建约束

按当前全局规则，默认不执行 `npm run build`。只有用户明确要求，或里程碑验收前获得用户认可后，才执行全量构建。

---

## 5.17 发布、部署与合规

### 发布前必须具备

1. 正式 README 和运行说明。
2. `.gitignore` 与干净的可追踪仓库。
3. 游戏内 Credits/许可证页面。
4. 静态部署方案和回滚方式。
5. 版本号、变更说明和已知问题。
6. 存档兼容策略。
7. 浏览器兼容和性能结果。
8. 外部试玩反馈入口。

### 授权风险

1. CC-BY 和 OGA-BY 素材不能只留在源码目录，正式产品内也要可访问署名。
2. 派生资源必须保留原始来源和处理方式。
3. 未来新增音频、字体和 Logo 同样需要记录授权。
4. 发布前建立一张“运行时实际使用资源清单”，而不是给整个素材仓库笼统署名。

---

## 6. 分阶段路线图

以下阶段只锁定先后关系，不在核心方向确认前伪造精确日期。

## 阶段 P0：基线固化

### 目标

得到一个经过真实运行验证的当前版本基线，明确“现在实际能玩到什么程度”和“哪些只是代码/配置已存在”。

### 范围

1. 完成 16 类感染体扩展的剩余运行时验收。
2. 修正扩展发现的阻塞问题。
3. 填写对应执行文档结果。
4. 确认是否保留全部 16 类进入后续内容池。
5. 建立 `.gitignore` 和仓库清理方案。
6. 确认 Git 仓库来源或重新初始化策略。
7. 更新过时资源 README 和长期文档状态。
8. 在真实浏览器中跑通当前 `GameScene`，记录加载、渲染、碰撞和场景跳转问题。
9. 至少完整试玩当前第一关一次，记录无法继续、节奏、速度、弹药和死亡原因。
10. 建立玩家与各类感染体的当前速度基线，优先处理用户已确认的“感染体移动过快”问题。

### 完成门槛

1. `npm run typecheck` 通过。
2. 三关、无尽、首页、武器库、怪物图鉴、设置均能进入。
3. 16 类感染体至少完成生成、动画、追击、受伤、死亡和对象池复用验证。
4. 没有阻塞级运行时异常。
5. 当前基线有可追溯记录。
6. 当前第一关可以从菜单进入并走到失败或通关结算。
7. 已形成第一份可玩性记录，而不是只保留截图和代码检查结果。
8. 已完成一轮感染体速度下调并通过实际试玩复核。

## 阶段 P1：方向锁定与预制作

### 目标

完成本文第 4 章的核心决策，并建立领域设计文档。

### 交付

1. 游戏玩法设计。
2. 武器设计。
3. 怪物与 Boss 设计。
4. 关卡设计。
5. 美术圣经。
6. 音频需求表。
7. 数值与测试基线。
8. 垂直切片范围说明。
9. 远程攻击与闪现攻击的行为规格。
10. 精细场景分层规范和第一关场景设计。

### 完成门槛

1. 一句话定位、目标玩家、平台、单局结构和非目标已确认。
2. 垂直切片内容数量已冻结。
3. 每个系统都有验收标准，而不只是功能清单。

## 阶段 P2：核心战斗与垂直切片

### 目标

做出一个能代表最终品质方向的完整关卡。

### 推荐内容

1. 1 个正式关卡。
2. 4 把差异清晰的武器。
3. 至少包含基础近战、远程攻击和闪现攻击的清晰敌人角色。
4. 1 个机制化 Boss。
5. 3 个打磨完成的环境战术元素。
6. 完整基础音效。
7. 正式地面、主要场景物、玩家与高频敌人表现。
8. 从菜单到结算的统一 UI。
9. 经试玩确认的玩家/感染体速度曲线。
10. 完整的实际场景分层、碰撞、环境细节和危险预警。

### 完成门槛

1. 外部试玩者可以独立完成一局。
2. 能明确说出至少三个项目记忆点。
3. 反馈问题主要集中在平衡偏好，而不是看不懂、听不清或无法操作。

## 阶段 P3：系统深度

### 目标

在垂直切片证明好玩后，增加可复玩深度。

### 候选范围

1. 局内轻量构筑。
2. 在首个远程与闪现原型基础上扩展控场、支援、冲锋等敌人行为。
3. 无尽事件和精英波。
4. 新环境机关。
5. 存档版本、挑战和记录体系。

所有候选必须重新立 Goal，不自动进入范围。

## 阶段 P4：内容生产

### 目标

在垂直切片通过后分批生产不少于 30 个正式闯关关卡。

### 推荐目标

1. 正式闯关关卡最终达到 30 关以上。
2. 建议按 5 关一个批次推进，共至少 6 个批次。
3. 每批引入或深化明确的场景主题、敌人机制和关卡目标。
4. 武器、战斗角色、Boss 和场景物数量根据关卡需要单独确认，不为凑数量强行扩展。
5. 无尽模式复用已经验证的正式内容，不反向拖慢闯关主线。

### 完成门槛

1. 不同关卡有不同打法和记忆点。
2. 内容扩展没有破坏数值、性能和视觉统一。
3. 所有正式内容通过同一套质量清单。
4. 30 关均完成实际运行与可玩性验收，不以配置文件中存在关卡条目作为完成标准。
5. 每个 5 关批次都有独立验收记录和回归结果。

## 阶段 P5：公开试玩

### 目标

用真实玩家反馈修正首次体验、平衡和兼容性。

### 交付

1. 可分享试玩版本。
2. 反馈表和已知问题。
3. 试玩数据记录。
4. 问题优先级和修复批次。

## 阶段 P6：发布候选

### 目标

形成包含 30 关以上闯关主线、可正式对外发布的完整作品。

### 完成门槛

1. 玩法、美术、音频、工程和合规均达到发布清单。
2. 无阻塞级问题。
3. 存档、部署和回滚方案已经验证。
4. Credits、许可证和版本说明齐全。

---

## 7. Goal + 文档执行方式

### 7.1 何时创建 Goal

只有满足以下条件才创建实现 Goal：

1. 对应方向已经讨论并形成明确决策。
2. 目标可以被一个可验证结果描述。
3. 范围和不做项已经冻结。
4. 依赖项已经完成或明确。

不要用 Goal 代替方向讨论，也不要同时开启多个相互依赖的 Goal。

### 7.2 标准流程

1. 在本文件登记待决策项。
2. 通过对话确认方案。
3. 把结论写入本文件的决策记录。
4. 创建一个具体 Goal。
5. 在 `docs/execution/` 建立执行文档。
6. 完成项目架构和调用链预读。
7. 按执行文档实施。
8. 进行最小必要验证。
9. 记录实际结果、偏离和剩余风险。
10. 更新本文件阶段状态。
11. Goal 达成后再标记完成。

### 7.3 Goal 命名示例

好的 Goal：

1. “完成第一关垂直切片，使新玩家能从主菜单进入、完成三段战斗并击败机制化 Boss。”
2. “为四把现有武器建立独立枪感、声音和资源职责，并通过固定试玩记录验证差异。”
3. “建立可版本迁移的本地存档结构，并兼容当前三类存档数据。”

不好的 Goal：

1. “完善游戏。”
2. “增加更多怪物。”
3. “优化一下美术。”

### 7.4 执行文档最低结构

每份复杂执行文档必须包含：

1. 目标。
2. 背景与已确认决策。
3. 范围。
4. 不做项。
5. 影响文件与调用链。
6. 清晰操作步骤。
7. 实施建议。
8. 潜在风险。
9. 优化方案。
10. 验证方式。
11. 执行结果。
12. 剩余风险与后续项。

---

## 8. 总体实施建议

1. 先稳定当前 16 类扩展，再讨论新增内容。
2. 先锁定产品形态，再决定是否加入 Roguelite 构筑。
3. 先把四把正式武器做出手感；四把测试配发武器不得因代码存在而自动计入正式内容。
4. 先把一个 Boss 做成机制战，再制作更多 Boss。
5. 先做一个正式关卡垂直切片，再批量生产关卡。
6. 美术优先替换每秒可见的世界对象，UI 细节放在其后。
7. 音频属于垂直切片必需项，不再长期后置。
8. 怪物扩充按“战斗角色”计数，不按配置条目计数。
9. 任何跨模块改动都先评估配置、实体、系统、场景、HUD、存档和测试。
10. 不为可能的联网、移动端或大地图提前污染当前架构。

---

## 9. 潜在风险分析

| 风险 | 当前表现 | 影响 | 对策 |
| --- | --- | --- | --- |
| 方向未锁定 | 旧文档同时存在街机、Roguelite、美漫矢量和像素路线 | 反复返工 | 先完成第 4 章决策 |
| 内容数量先于机制 | 已接入冲刺、远程、死亡爆炸和 Boss 范围技能，仍有部分近战数值亚种 | 同类敌人可能继续同质化 | 继续区分视觉亚种与战斗角色，并用试玩验证组合价值 |
| 视觉拼贴 | 多作者、多像素密度、多视角资源共存；武器已统一为单一素材包并与图鉴共用，角色与感染体仍跨包 | 成品感不足 | 建立美术白名单与 ART_BIBLE，按武器的做法逐类收敛到单一来源 |
| 静态检查失效 | `npm run typecheck` 与 `npm run build` 当前因测试文件缺 `@types/node` 而失败 | 类型回归无法拦截，发布构建不可用 | 作为 P0 阻塞项单独立项修复，修完恢复「每次改代码后跑 L1」流程 |
| Boss 数值化 | 两个 Boss 已有震荡/轰炸技能和生命条，但尚未完成阶段变化与试玩 | 高潮仍可能不足 | 浏览器实测技能节奏，再补阶段变化和专属资产 |
| 音频质量 | 开放授权音频集、事件映射和处理台账已接线，缺主观混音与多浏览器听感验收 | 不同设备上的响度和辨识度仍可能不稳定 | 完成关卡实听，按事件表调整音量、速率和并发，不再回退到合成占位音 |
| `GameScene` 膨胀 | 超过 700 行且继续承接新功能 | 修改风险上升 | 在新行为系统落地时定向拆分 |
| 无尽性能 | 已使用空间哈希并限制未使用场景物数量，尚未完成 50/100/150 敌人压力测试 | 后期仍可能掉帧 | 建立性能门槛并记录真实帧率 |
| 数值失控 | 新敌人、波次和掉落同时变化 | 难度与经济不可解释 | 建立平衡统计和固定试玩记录 |
| 存档漂移 | 已有版本、归一化、迁移与重置，缺真实历史浏览器数据验证 | 极端旧数据仍可能异常 | 用历史样本验证并补迁移用例 |
| 文档漂移 | 多份计划与代码状态冲突 | 决策不可追踪 | 本文件作为唯一总入口 |
| 仓库卫生 | Git 与 `.gitignore` 已存在，但调试资料、日志和工作区依赖仍较多 | 发布内容可能混入临时文件 | 发布前建立实际追踪清单并清理 |
| 授权遗漏 | CC-BY/OGA-BY 仅在源码中记录 | 发布合规风险 | 游戏内 Credits + 实际使用清单 |
| 范围膨胀 | 武器、怪物、关卡候选很多 | 无法形成完整版本 | 以垂直切片门槛控制扩张 |

---

## 10. 优化方案

### 10.1 规划优化

1. 用决策编号管理跨文档结论。
2. 近期任务拆到可验收级，远期只保留里程碑。
3. 每阶段结束必须有可玩的版本。
4. 新想法先进入候选池，不直接插入当前 Goal。

### 10.2 内容优化

1. 复用行为时，把差异定义为视觉亚种，不伪装成新机制。
2. 关卡优先通过组合和布局复用系统。
3. 武器优先扩职责，不扩同质数值档位。
4. Boss 资产与机制共同设计，避免逻辑完成后再硬贴美术。

### 10.3 工程优化

1. 为配置建立自动完整性检查。
2. 为纯逻辑建立少量高价值测试。
3. 给性能优化设置真实触发阈值。
4. 原始素材、派生素材和运行时资源分层。
5. 清理临时文件前先恢复版本控制与忽略规则。

---

## 11. 本文档验证记录

### 已完成

1. 阅读 `package.json`、TypeScript/Vite 配置和入口文件。
2. 阅读全部配置、实体、系统、场景、工具和资源处理脚本。
3. 阅读现有总计划、长期目标、路线图、测试说明和阶段执行文档。
4. 核对素材目录、来源、授权、处理后资源和运行时导入。
5. 启动 Vite 开发服务器，确认服务可启动。
6. `[废弃]` 原记录「在感染体扩展接线完成后重新执行 `npm run typecheck`，结果通过」。2026-07-30 复核为失败，原因见 §3.4 第 7 条。
7. 2026-07-30：`npm test` 通过（7 文件 26 项）。
8. 2026-07-30：Git 元数据可读，已核对当前工作区差异与提交历史（当前仅 1 个提交 `chore: initialize repository`，其余为未提交改动）。
9. 2026-07-30：headless Chrome 147 + CDP 完成第一关运行时冒烟与武器表现验收，运行时异常 0；详见 `docs/execution/2026-07-30-real-weapon-models.md`。

### 未完成

1. 未在有头浏览器中完成完整交互试玩；headless 只能证明不崩与静态构图。
2. 未完成 16 类感染体逐项动画和图鉴截图验证。
3. 未执行 `npm run build`；除高成本约束外，该命令当前还会因 §3.4 第 7 条的类型错误直接失败。
4. 未完成 50/100/150 活跃敌人的无尽模式性能压测与第一关完整可玩性记录。
5. 未完成武器斜向持枪贴合度与有头浏览器观感验收。

### 当前结论

项目已不是空白原型，而是一个功能链路较完整、内容和资产正在扩张的可玩项目。当前主要问题已经从“能不能运行”转为：

1. 最终产品形态尚未锁定。
2. 内容数量开始快于机制深度。
3. 美术与文档需要统一。
4. 音频、平衡、测试和发布治理明显滞后。
5. 必须先建立稳定基线，再进入下一轮扩张。

---

## 12. 决策记录

| 编号 | 议题 | 当前结论 | 状态 |
| --- | --- | --- | --- |
| D-001 | 产品形态 | 长期关卡闯关为主体，最终不少于 30 关；无尽保留为副模式；局内构筑另作独立决策 | 已确认（依 §4.1） |
| D-002 | 单局长度 | 关卡 8-15 分钟，无尽 3 分钟进入压力 | 待确认 |
| D-003 | 美术方向 | 全局统一像素风格；美漫矢量方案废弃并删除文档 | 已确认（依 §4.3，2026-08-05） |
| D-004 | 感染体定义 | 必须靠攻击方式与行为改变玩家决策；视觉亚种与战斗角色分开计数 | 已确认（依 §4.4） |
| D-005 | 地图规模 | 近期保持固定单屏竞技场 | 待确认 |
| D-006 | 首发规模 | 正式闯关主线最终不少于 30 关，按每批 5 关推进；武器/战斗角色/Boss 数量待确认 | 已确认（依 §4.6） |
| D-007 | 局外成长 | 解锁内容，不永久增加基础属性 | 待确认 |
| D-008 | 后端与联机 | 垂直切片和首发阶段均不做 | 待确认 |

> 表内 D-001、D-004、D-006 原标记为「待确认」，与第 4 章正文的 `[已确认]` 冲突。2026-07-30 按 §1.3 事实优先级以正文为准同步为已确认。

---

## 13. 下一轮讨论顺序

为了避免同时讨论过多细节，建议按以下顺序逐轮确认：

1. ~~**产品形态**：纯街机闯关，还是轻量 Roguelite。~~ 已确认为长期关卡闯关，见 D-001。
2. **单局结构**：波次、局内选择、Boss、结算和解锁。
3. ~~**美术方向**：像素战场与漫画表达的边界。~~ 已确认为全局统一像素风格，见 D-003。
4. **怪物体系**：现有 16 类如何归并为战斗角色与视觉亚种。
5. **武器体系**：4 把正式武器与 4 把测试配发武器的职责、获取和正式入池顺序。武器视觉签名已完成，本项只讨论职责与平衡。
6. **关卡体系**：三关原型如何变成正式关卡。
7. **垂直切片**：冻结 P2 的具体范围并创建第一个 Goal。

下一轮讨论第 2 项「单局结构」。确认后更新 D-002，再进入下一项。

在进入方向讨论之前，建议先清掉一个阻塞项：`npm run typecheck` 与 `npm run build` 当前处于失败状态（见 §3.4 第 7 条），这是 P0 完成门槛第 1 条，且不涉及任何方向决策。
