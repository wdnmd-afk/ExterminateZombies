# 2026-08-12 G3-2 爽感专属音效事件

> 状态：事件链已实施；当前使用已入库 CC0 素材临时映射，专属素材与 V6 实听待完成

## 1. 实施内容

1. 新增 `critical`、`execute`、`pierce`、`streak`、`heartbeat` 五个语义事件。
2. 暴击、处决、M4A1 穿透播报和连杀里程碑现在分别触发独立事件名，后续替换音频文件无需再改战斗逻辑。
3. 当前映射复用已登记的金属命中、爆炸、Boss/波次 UI 和玩家受伤素材，保持限频、并发和优先级混音链路。
4. `heartbeat` 已登记为 G1-7 依赖，但尚未接入濒死机制，避免在无濒死状态下循环播放。

## 2. 素材与合规

本批没有下载新素材，全部复用 `src/assets/processed/audio/` 中已有并登记的文件；专属 stinger 和心跳循环仍需从 G3 候选 CC0 包按台账规则核对后替换，当前不能标记为最终音效完成。

## 3. 验证

- `npm test` 与 `npm run typecheck`：收口时执行。
- 浏览器触发链和 V6 耳机/扬声器实听：待后续 P2 回归与 G3-4。
