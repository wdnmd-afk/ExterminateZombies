# 主目标与下一步计划

> 文档类型：滚动更新的执行状态与下一步计划
> 更新日期：2026-09-15
> 当前基线：`42e64cd`（分支 `main`，已推送）+ 未提交改动
> （`src/systems/EffectSpritePool.ts` 关停防御、`tests/effect-sprite-pool-shutdown.test.ts`、本轮文档与证据）
>
> **本文件是「下一步做什么」的唯一入口。每完成一步任务必须更新本文件**：
> 勾掉已完成项、回填实测证据路径、把下一步移到 §3 顶部。
> 只写状态与计划，不重复各执行文档已有的实施细节。

## 1. 主目标

把项目推进到 `PROJECT_MASTER_PLAN.md` 的 **P2 完成门槛**：一个能代表最终品质方向的完整关卡，
且外部试玩者可以独立完成一局并说出至少三个记忆点。

当前阻塞 P2 的**不是代码功能缺失**，而是验证层级：绝大多数功能停在 V0-V2（静态审阅 + 命令层），
V3-V6（浏览器实景 / 完整试玩 / 真人验收）几乎全项目空白。

因此近期主目标不是加内容，而是**按统一验收清单逐项收口实景验证**。

## 2. 当前状态

### 2.1 阶段状态

| 阶段 | 状态 | 阻塞点 |
| --- | --- | --- |
| P0 基线固化 | 已完成 | — |
| P1 方向锁定 | 已完成 | — |
| **P2 垂直切片** | 客观项基本完成，**门槛未达成** | 外部试玩从未进行；三个记忆点依赖 V6；G5-2 第二关 V5/V6 未做 |
| P3 系统深度 | 实施中，**在 P2 门槛未过时提前展开** | G6-6 生产数值与独立剧本、G3-4 主观混音、G5-4 剩余 UI |
| P4 内容生产 | 模板已沉淀，批量生产未开始 | 距 30 关目标差 20 关；第 3-7 关最终数值与独立剧本缺失 |
| P5 公开试玩 | 未开始 | `dist/` 从未在浏览器加载过，单包 1.93 MB 未做代码分割 |
| P6 发布候选 | 未开始 | 根目录源码 LICENSE 待决策、部分 AI 原图归档、发布包许可交付 |

注意 `PROJECT_MASTER_PLAN.md` §8 要求「任何 Goal 未通过验收不开启依赖它的下一项」，
但 P3/P4 已大量落地而 P2 门槛未过。**当前累积的未验证面积主要来自这个倒挂，不要继续扩大它。**

### 2.2 命令层（已收口）

| 层级 | 结果 |
| --- | --- |
| V1 全量 | **41 文件 / 673 用例通过**（2026-09-15，含 U-15 轮新增 3 例关停用例） |
| V2 类型检查 | **退出码 0** |
| V2 构建 | 未执行（需单独申请） |

计数口径：673 是当前全量实测值。历史文档里的 670、417、646、83 等是各自时点的记录，**不可相加**。

### 2.3 实景层（已收口三条）

| 用例 | 状态 | 证据 |
| --- | --- | --- |
| U-14 固定关卡连杀返还 | **V3/V4 通过，四条判据全过** | `docs/execution/evidence/2026-09-14-u14-streak-reward/` |
| U-15 连杀奖励模式隔离 | **V3/V4 通过，九条判据全过**（仅 10 连杀档，20/35 档未实景） | `docs/execution/evidence/2026-09-14-u15-mode-isolation/` |
| U-17 环境连锁 + 广播调度 | **V3/V4 通过，四条判据全过** | `docs/execution/evidence/2026-09-14-u17-lure-chain/` |
| U-01～U-13、U-16 | 待执行 | — |
| 全部 V6 真人验收 | 待执行（Agent 不能代替） | — |

## 3. 下一步计划

按顺序执行。每完成一项，回到本文件勾掉并回填证据路径。

### 3.1 下一步：U-16 构筑可见性

- 两次及以上抽卡之间观察卡面流派徽标、`当前构筑` 摘要和武器层数
- 判据：流派徽标与卡的真实 `effects` 一致；层数与已激活卡一致且排序稳定；
  首次强化不显示空摘要；长文案不压卡片或底部跳过按钮
- 空构筑与脏 id 路径各检查一次

### 3.2 炮弹反打实景（`4621d2a` 剩余缺口）

单测已覆盖「同一弹体只能反打一次」和令牌复用，但以下依赖 Phaser 生命周期，只能实景验证：

- 散弹同帧命中、贴身反打、撞墙、射程终止
- 源 Boss 死亡后回池复用
- 地面红圈不可反打、伤害归属
- `prop-lure-station.png` 等两张位图的实际加载（尚未计入资源清单快照）

### 3.3 P2 门槛攻坚（需真人）

Agent 无法代替，必须真人执行：

- 外部试玩者独立完成一局
- 能说出至少三个项目记忆点
- G5-2 第二关的 V5 完整试玩与 V6 真人目视
- G3-4 主观混音（耳机 + 扬声器两轮，Chrome/Edge/Firefox）

### 3.4 长期未收口项（独立于上述顺序）

- 审计缺口 A：击杀来源未入连杀链
- 审计缺口 B：`utils/ObjectPool.ts` 无 `maxSize`，是唯一没有硬上限的显示资源，需先取实景峰值
- 仓库体积 919 MB（`.git` 351.5 MB + `src/assets/generated` 170.8 MB 跟踪 AI 原始输出）
- 根目录源码 `LICENSE` 待用户决策

## 4. 执行约束

1. **授权**：`TESTING_RULES.md` §3.5 有常设授权覆盖 V1-V5 本地命令，但用户全局规则要求测试 /
   类型检查 / 构建逐轮申请，且按 §1 优先级高于 §3.5。**每轮执行命令前先申请**，构建与浏览器单独申请。
2. **结论口径**：按 §13 分级。Agent 只能给出「客观通过，主观待验」，爽感、难度、混音必须真人回填。
3. **不猜字段**：探针只用源码已确认的公开访问器。私有字段按 §10.2 默认不读。
4. **证据留存**：放 `docs/execution/evidence/<日期>-<用例>/`，不删除，由用户决定清理。
   注意 `.gitignore` 的 `*.log` 会挡住日志文件，关键读数要同时落在 JSON 里。
5. **清理**：只停止本轮自己启动的进程，不动用户已有的服务器和浏览器。

## 5. 实景驱动经验（复用可省大量时间）

已跑通的 CDP 驱动在 `.debug-u14-*.mjs`、`.debug-u15-*.mjs`、`.debug-u17-chain.mjs`
（受 `.gitignore` 的 `.debug-*` 保护，不入库）。要点：

1. **必须先解除后台节流**，否则 Phaser 场景状态机永不推进。启动参数三个 `--disable-*`
   加会话内 `Emulation.setFocusEmulationEnabled`；`Page.bringToFront` 不足以替代。详见 §9.3.1。
2. **逻辑画布宽度不是 1280，而且每轮都可能不同**。`DisplayManager` 为固定侧栏动态加宽，
   U-14 实测 1520、U-15 实测 1734，随窗口尺寸变化。按 1280 换算点击会落到错误的行——
   首次实测点「第二关」命中了「第十关」。必须每轮从 `scale.gameSize` 读实际值，
   战场内还要叠加 `camera.x` 偏移（U-15 为 227），不能沿用上一轮的数字
   （已补正进 `TESTING_RULES.md` §10.1）。
3. **菜单行带入场 tween**，动画未停时坐标在移动。`tweens.killAll()` 会把它**冻结在中途**而非跳到终点，
   读到的仍是错坐标。正确做法是轮询到坐标连续两次稳定再点，并校验 `selectedLevelId`。
4. **暂停期探针读数是中间态**。`getSkillStatus()` 用 `scene.time.now`，而 `shiftBattleTimers`
   的平移量用 `game.loop.time`，暂停时 `timeScale=0` 使两者基准不同。
   判别法：改变暂停时长，看残差是否同比增长——不增长就是平移生效，不是冷却泄漏。
5. **让状态自己满足条件，别追求精确时序**。U-14 花了六次才成功：前五次都在试图让「一次技能施放」
   精确对上「一次连杀爆发」，而爆发时刻不可预测。正解是**技能一就绪就立刻放**，
   使冷却覆盖率接近 100%，任何里程碑都必然落在窗口内。
6. **抽卡界面会吃掉 ESC，这是产品的正确行为**。`CardSelectionScene` 的 ESC 是「跳过本次强化」，
   且跳过后 `pendingEnhancementPacks` 会立刻弹下一包，所以队列非空时连按 ESC 只是一路跳卡。
   进暂停前先用 `Digit1` 显式收口抽卡队列，不要靠多按 ESC 试。
7. **`pause=null` 有两种真因**，日志里长得一样：抽卡界面拦截，或玩家已阵亡
   （`handleMenuKey` 首行就 `if (event.repeat || this.gameEnded) return`）。
   驱动必须同时打印活跃场景 / 血量 / 结算场景，否则会把死亡误报成暂停键故障。
8. **无尽模式连杀窗口出现得比预期晚**。10 连杀在第 4 波（约 14 只）密度不够，
   实测是第 7 波（约 20 只）才凑齐，无尽段耗时约 204s 游戏内时间，预算要留够。

## 6. 更新记录

| 日期 | 完成项 | 结论 | 证据 |
| --- | --- | --- | --- |
| 2026-09-14 | 尸潮调度与炮弹反打纯规则用例（24 例） | V1/V2 通过 | 提交 `d28cf43` |
| 2026-09-14 | U-14 固定关卡连杀返还 V3/V4 | 客观通过，主观待验 | `evidence/2026-09-14-u14-streak-reward/` |
| 2026-09-14 | `TESTING_RULES.md` 补正至 1.5 | 逻辑宽换算 + 探针时钟陷阱 | 提交 `07413fb` |
| 2026-09-14 | U-17 环境连锁 + 广播调度 V3/V4 | 客观通过，主观待验 | `evidence/2026-09-14-u17-lure-chain/` |
| 2026-09-15 | U-15 连杀奖励模式隔离 V3/V4（九条判据） | 客观通过，主观待验 | `evidence/2026-09-14-u15-mode-isolation/` |
| 2026-09-15 | 修复关停期 `EffectSpritePool.release` 抛异常（阻断下一关启动） | V1/V2 通过，实景同路径 `exceptions=0` | `src/systems/EffectSpritePool.ts` + `tests/effect-sprite-pool-shutdown.test.ts` |
