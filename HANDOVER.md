# 交接文档：2026-09-08 工作区现状

> 面向对象：下一个接手本项目的 Agent
>
> 生成日期：2026-09-07；更新日期：2026-09-08
>
> 对应已推送基线：`bc75e37`（`main`）；本批 G7 治理改动尚未提交，尸潮调度 / 炮弹反打为并发工作区改动，独立保留。
>
> 文档定位：说明**当前哪些已完成、哪些只完成了一半、哪些完全没做**，避免把"已实施"误读为"已验证通过"。
>
> 上一版本文档停在 2026-08-13（基线 `e92d51c`），已落后约 50 个提交，本版整体重写。

## 1. 一句话现状

内容规模已到 17 武器 / 5 角色 / 51 强化卡 / 18 感染体档案 / 10 关 + 无尽；危墙、稳定性、粒子/UI、环境派生、教学、G6-6 门禁与档案页公共组件已推送至 `bc75e37`。本批 G7 补录 217 文件清单 / 指纹、ART/AUDIO 与官方字体法律声明，修正旧署名；部分 AI 采用原图归档仍待处理。上轮 G6-6 的 83 个定向用例与类型检查不覆盖后续 UI 或当前完整工作区；本批只做静态资源核对，没有测试、构建或实景验收。

## 2. 近期提交的真实状态

| 提交 | 内容 | 实现 | 验证 | 执行文档 |
| --- | --- | --- | --- | --- |
| `7270f2b` | 武器改关卡奖励解锁、四个战术道具、Boss 血量 ×0.65 | 已完成 | V0-V2 通过；**V3-V6 全部未执行** | `docs/execution/2026-08-31-weapon-level-reward-and-tactical-items.md` |
| `9c69a3e` | 设置页与武器库行网格错位修复 | 已完成 | 命令层通过 | — |
| `e3df971` | 粉尘/寒雾残留区位图化（第 9 张特效序列帧） | 已完成 | 命令层 + CDP 交付链探针五级全通；**目视验收未完成** | `docs/execution/2026-08-31-dust-zone-bitmap.md` |
| `06e2d4d` | 第三关危墙坍塌试用、120 只分段敌群 | 代码已提交 | 历史命令基线见第 5 章；**实景与真人验收仍待执行** | `docs/execution/2026-09-03-level3-breakable-walls.md` |
| `bc75e37` | 后续稳定性、粒子/UI、环境派生、教学、G6-6 门禁与档案页组件 | 已推送 `main` | G6-6 有历史定向证据，档案页仅 V0；不等于完整回归通过 | `docs/execution/2026-09-07-followup-goals.md` |

## 3. 必须优先处理的遗留风险

### 3.1 `7270f2b` 修的是一个"命令层查不出"的缺陷，而它自己只有命令层证据

该轮修掉的缺陷是：`ZOMBIES` 里有 20 条 `type: 'weapon'` 死掉落，导致除沙漠之鹰外 **16 把武器整局都拿不到**，运行时静默无日志。修复方式是改成关卡阶段奖励确定性交付。

关键点：这个缺陷当时被 373 个绿灯完整掩盖，因为 `config-integrity.test.ts` 与 `weapon-loadout.test.ts` 只断言"配置存在且自洽"——配置确实自洽，只是它描述的获取路径在运行时不通。

因此**下一轮的第一条验证必须是**：从第一关走到第二关，确认 MP5 真的到手。这直接验证缺陷是否真的修好了，命令层无法替代。

同轮还有三项未实机确认：四张道具贴图是否被正确加载显示、四种区域效果（地火/硬停/高爆/减速）的实际表现、Boss 新血量的真实 TTK（26-33 秒是推算值，未重测）。

### 3.2 血液与火花位图接线仍缺实景证据

G5-3 是**部分完成**，不是未开始：爆炸、烟尘、枪口焰×3、火焰×3、粉尘共 9 张序列帧位图已接线。2026-09-07 血液、命中火花与爆炸火花已接入静态粒子位图池，但可访问性设置、暂停/关停和实际画面仍需统一浏览器验收。

### 3.3 仓库体积

总计约 919 MB：`.git` 351.5 MB、`src/assets/generated` 170.8 MB（跟踪的 AI 原始生成输出）、`src/assets/processed` 81.5 MB。原始生成图入库是主因，且随每轮美术生成持续膨胀。尚未处理。

## 4. 本轮（2026-09-01）已修的三处问题

1. **`npm test` 在本机恒定失败**：`e3df971` 误把 `tests/__tmp_analysis.test.ts` 与 `tests/__tmp_out.txt` 提交入库。前者在模块顶层 `writeFileSync` 到硬编码的 `G:/github/...`，而 vitest 的 `include` 是 `tests/**/*.test.ts` 会收集它，本机无 G 盘 → 采集期 ENOENT。已从跟踪与工作区移除，并在 `.gitignore` 增加 `tests/__tmp_*` 防复发。移除后 32 文件 / 400 用例全绿。
   > 顺带事实：上一轮工作是在 `G:` 盘的另一份副本上进行的，存在两份工作副本。
2. **`HANDOVER.md` 漂移**：即本文档，已整体重写到 `e3df971`。
3. **G 目标基线口径错误**：`LONG_TERM_OPTIMIZATION_GOALS.md` §0.3 原写"爆炸/烟尘/血液特效仍为程序图形（G5-3 未开始，无序列帧）"，与磁盘上 9 张特效位图矛盾。已按 §3.2 的实际情况改写，并补齐 §5.2 G5 任务表缺失的状态列（原表头两列、G5-1 行三格，本身错位）。

## 5. 命令验证记录

| 层级 | 命令 | 结果与覆盖范围 |
| --- | --- | --- |
| V0 | 档案页源码、调用链、差异与空白核对 | 2026-09-08 已核对动态引用、图层组装、原业务函数与键盘监听清理；不代表命令或实景验证通过 |
| V0 | G7 资源登记与官方许可取证 | 217 文件的加载 / 来源 / 元数据 / 指纹核对；官方字体同字节与中英文声明归档；不代表 Credits 展示、解码或发布包通过 |
| V1 | `npm test` | 历史记录：34 文件 / 417 用例通过；本次续接没有重跑全量测试 |
| V1 定向 | `npm test -- tests/level-batch-validation.test.ts` | **通过：1 文件 / 83 用例（2026-09-07，用户授权后）** |
| V2 | `npm run typecheck` | **2026-09-07 基线通过：退出码 0（修正测试夹具断言后）**；不覆盖 2026-09-08 档案页改动 |
| V2 | `npm run build` | 未执行（按 C-9 需单独申请） |
| V3-V6 | 浏览器冒烟 / Agent 实景 / 完整试玩 / 真人验收 | **未执行** |

> 授权口径：历史长期授权见 `LONG_TERM_OPTIMIZATION_GOALS.md` §9 C-9；本次续接以用户最新规则为准，测试、类型检查、构建和浏览器均需另行明确同意。执行前参照 `TESTING_RULES.md`。

> 上轮用户授权仅覆盖 G6-6 门禁定向测试与类型检查，不自动扩展到本批 G4-6。`tests/archive-layout.test.ts` 已写入但未运行，当前类型检查也未重跑；83 个定向用例不能与历史 417 用例相加后宣称全量通过。

## 6. 真人主观验收积压（V6，Agent 不能代替）

1. 后四把武器（AK-47 / Barrett / RPG-7 / M79）能否各自说出"最爽瞬间"；Barrett 伤害 210 是否过高。
2. Boss TTK 是否落在舒适区间——`7270f2b` 的 ×0.65 是保守推算，未实测。
3. G3-4 混音：耳机 + 扬声器两轮实听、多浏览器解码复核。
4. 51 张强化卡的爽感（G2-7 客观验收已过，V6 待做）。
5. 粉尘位图"实机上好不好看"（`e3df971` 目视验收未完成）。

## 7. 当前未完成项

1. **G5-2 第二关正式位图环境**：历史客观 V0-V4 已有证据，V5 完整试玩与 V6 真人目视仍未执行；其余九关与无尽已接入 G5-5 调色派生位图，尚无本批次实景证据。
2. **G5-3 特效位图化**：血液/命中火花/爆炸火花已接入静态位图池，统一浏览器视觉验收仍待执行。
3. **G4-6 / G5-4**：已有按钮、准星和按键帽接线；武器库/图鉴新增公共背景、横幅、列表、详情外壳、页脚与导航，武器库 Enter/空格编队复用原校验，两页 Home/End 与监听清理已接入。档案页待命令/实景验证，其他页面组件与剩余 UI 位图仍待补齐。
4. **G5-5 环境批量化**：11 个主题的位图三件套和调色派生已接入，完整实景/长时视觉验收待统一批次；**G6-6 批量扩关**已有五关模板、全关卡剧本与排程门禁及变异用例，第 3-7 关最终数值和独立剧本仍待实施。
5. **G6-4 教学关升级**：第一关已补 MP5→SPAS 奖励节奏和一次强化，完整教学验收未执行。
6. **G3-4 主观混音**、Boss TTK/V6、第三关危墙实景与本次 G6-6 门禁回归：按 `docs/execution/2026-09-07-unified-acceptance.md` 分别执行，不能沿用历史结论。
7. **G7-3 采用原图归档**：三张 AI 重火力侧视原图未在本工作区找到；四张战术道具 `v01` 候选仍在 `TmpGenerate/`、未受版本控制。需要确认采用版本并稳定归档，不能重新生图冒充历史来源。
8. **已知代码风险**：召唤账本与对象池关停已修复；统一验证仍需确认 Phaser 场景生命周期和对象池回收路径。
9. **P6 发布许可**：普惠体官方中英文全文已归档且 WOFF2 与官方同字节，不再缺原文；根级源码 LICENSE 仍待用户决定，发布包携带许可材料与额外分发安排仍需确认。

### 本批 G7 已同步的登记

- `docs/RUNTIME_ASSET_MANIFEST.md` 与 `docs/RUNTIME_ASSET_INVENTORY.csv` 固定在 `bc75e37`：164 图片、52 音频、1 字体；8 张旧感染体仍预载，保留许可，不与当前实体视觉混用。
- 两张粒子、两张 UI、30 张环境调色派生及早前漏登的感染体 / Boss / 武器 / 道具均已补录；项目生成不误记为 CC0。
- 字体取证链、中英文全文、原始正文快照与哈希见 `src/assets/downloaded/fonts/alibaba-puhuiti-3/SOURCE.md`；本批未替换字体或改变玩法。
- 并发 `prop-lure-station.png` 等资源不计入本快照，待所属批次接线完成后更新。执行记录见 `docs/execution/2026-09-08-g7-asset-governance.md`。

## 8. 流程提醒

`7270f2b` 提交时只改了 `README.md`，未按 `PROJECT_MASTER_PLAN.md` §7.2 建执行文档、未同步 G 目标表，导致目标表与代码在 Boss 血量和武器数量两处对不上，随后靠 `62f1a8c` 补录。约束存在但执行不稳，**每个 Goal 收口必须同步执行文档 + G 目标表 + 相关台账**（G7-3）。

本次第三关改动已建立独立设计文档和执行文档；未调用图片 API，继续复用现有障碍位图。后续必须先完成 V1/V2，再从主菜单真实进入第三关完成 V3-V5，最后由真人判断“坍塌压群并打开路线”是否构成明确爽点。

本次 G7 没有重做危墙、改变关卡数值或覆盖并发改动。G6-6 记录见 `docs/execution/2026-09-07-g6-6-batch-template.md` §8-14；G4-6 记录见 `docs/execution/2026-09-07-g4-6-archive-components.md`。资源登记和字体原文已补齐，但采用原图归档、其他页面组件、批量生产、发布与实景/真人验收仍有未完成项。
